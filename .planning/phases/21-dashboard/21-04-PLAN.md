---
phase: 21-dashboard
plan: 04
type: execute
wave: 2
depends_on: [21-03]
files_modified:
  - buygo-plus-one-dev/admin/css/dashboard.css
autonomous: true

must_haves:
  truths:
    - "統計卡片在桌面版 4 欄排列，手機版 1 欄排列"
    - "圖表卡片在桌面版 2 欄排列，手機版 1 欄排列"
    - "統計卡片 hover 有陰影效果"
    - "變化百分比正數顯示綠色，負數顯示紅色"
    - "圖表容器高度桌面版 300px，手機版 250px"
    - "所有樣式使用設計系統 tokens（顏色、間距、字體）"
  artifacts:
    - path: "buygo-plus-one-dev/admin/css/dashboard.css"
      provides: "Dashboard 頁面專用樣式"
      min_lines: 100
      contains: ["stats-grid", "stat-card", "chart-card", "chart-container", "activities-list"]
  key_links:
    - from: "dashboard.css"
      to: "design-system/tokens/"
      via: "使用 CSS 變數"
      pattern: "var\\(--spacing-.*\\)|var\\(--color-.*\\)"
---

<objective>
建立 dashboard.css - Dashboard 專用樣式

Purpose: 實作 Dashboard 頁面的視覺樣式，確保桌面版和手機版響應式佈局
Output: dashboard.css 檔案，包含統計卡片、圖表、活動列表的完整樣式
</objective>

<execution_context>
@/Users/fishtv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fishtv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/fishtv/Development/.planning/ROADMAP.md
@/Users/fishtv/Development/buygo-plus-one-dev/.planning/phases/21-dashboard/TECH-SOLUTION.md

# 參考設計系統
@/Users/fishtv/Development/buygo-plus-one-dev/design-system/tokens/colors.css
@/Users/fishtv/Development/buygo-plus-one-dev/design-system/tokens/spacing.css
@/Users/fishtv/Development/buygo-plus-one-dev/design-system/tokens/typography.css
@/Users/fishtv/Development/buygo-plus-one-dev/design-system/components/card.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 dashboard.css 並定義統計卡片網格</name>
  <files>buygo-plus-one-dev/admin/css/dashboard.css</files>
  <action>
建立 `dashboard.css` 檔案，定義統計卡片網格樣式：

1. 建立 `buygo-plus-one-dev/admin/css/dashboard.css`
2. 定義統計卡片網格佈局：

```css
/* 統計卡片網格 */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-4);
    margin-bottom: var(--spacing-6);
}

/* 響應式：手機版垂直排列 */
@media (max-width: 768px) {
    .stats-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-3);
    }
}

/* 統計卡片 */
.stat-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--spacing-4);
    transition: all var(--transition-base);
}

.stat-card:hover {
    border-color: var(--color-hover-border);
    box-shadow: var(--shadow-md);
}

/* 統計卡片內容 */
.stat-card-label {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-2);
}

.stat-card-value {
    font-size: var(--font-size-3xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-primary);
    margin-bottom: var(--spacing-1);
}

.stat-card-change {
    font-size: var(--font-size-xs);
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-1);
}

/* 變化百分比顏色 */
.stat-card-change.positive {
    color: var(--color-success);
}

.stat-card-change.negative {
    color: var(--color-danger);
}
```

**重要技術約束:**
- 所有顏色使用設計系統 tokens（`var(--color-*)`）
- 所有間距使用設計系統 tokens（`var(--spacing-*)`）
- 所有字體大小使用設計系統 tokens（`var(--font-size-*)`）
- 響應式斷點為 768px（與現有頁面一致）

參考 `TECH-SOLUTION.md` 第 4.2 節的樣式設計。
  </action>
  <verify>
載入 Dashboard 頁面，檢查：
- 桌面版統計卡片 4 欄排列
- 手機版統計卡片 1 欄排列
- Hover 效果正常
- 變化百分比顏色正確
  </verify>
  <done>
統計卡片網格樣式完成，響應式佈局正常，使用設計系統 tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: 定義圖表卡片和圖表容器樣式</name>
  <files>buygo-plus-one-dev/admin/css/dashboard.css</files>
  <action>
在 `dashboard.css` 中加入圖表相關樣式：

```css
/* 圖表網格 */
.charts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: var(--spacing-4);
    margin-bottom: var(--spacing-6);
}

/* 響應式：手機版單欄 */
@media (max-width: 768px) {
    .charts-grid {
        grid-template-columns: 1fr;
    }
}

/* 圖表卡片 */
.chart-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--spacing-4);
}

.chart-title {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-3);
}

/* 圖表容器 */
.chart-container {
    position: relative;
    height: 300px;
}

/* 響應式：手機版降低高度 */
@media (max-width: 768px) {
    .chart-container {
        height: 250px;
    }
}
```

**重要技術約束:**
- 圖表容器使用 `position: relative` 配合 Chart.js 的 `maintainAspectRatio: false`
- 高度固定（桌面 300px，手機 250px）確保圖表可讀性
- 最小寬度 400px 防止圖表過窄
  </action>
  <verify>
載入 Dashboard 頁面，檢查：
- 圖表卡片正確顯示
- 圖表容器高度正確（桌面 300px，手機 250px）
- Chart.js 圖表填滿容器
  </verify>
  <done>
圖表卡片樣式完成，圖表容器高度響應式正確
  </done>
</task>

<task type="auto">
  <name>Task 3: 定義活動列表和商品概覽樣式</name>
  <files>buygo-plus-one-dev/admin/css/dashboard.css</files>
  <action>
在 `dashboard.css` 中加入活動列表和商品概覽樣式：

```css
/* 活動列表 */
.activities-list {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--spacing-4);
    margin-bottom: var(--spacing-6);
}

.activities-list h3 {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    margin-bottom: var(--spacing-4);
}

/* 活動項目 */
.activity-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-3);
    padding: var(--spacing-3);
    border-bottom: 1px solid var(--color-border);
}

.activity-item:last-child {
    border-bottom: none;
}

.activity-icon {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.activity-icon.order {
    background: rgba(59, 130, 246, 0.1);
    color: var(--color-primary);
}

.activity-icon.customer {
    background: rgba(16, 185, 129, 0.1);
    color: var(--color-success);
}

.activity-content {
    flex: 1;
}

.activity-title {
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-1);
}

.activity-description {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
}

.activity-time {
    font-size: var(--font-size-xs);
    color: var(--color-text-tertiary);
    flex-shrink: 0;
}

/* 響應式：手機版 */
@media (max-width: 768px) {
    .activity-item {
        flex-direction: column;
        align-items: flex-start;
    }

    .activity-time {
        align-self: flex-end;
    }
}

/* 商品概覽 */
.products-overview {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--spacing-4);
    margin-bottom: var(--spacing-6);
}

.products-overview h3 {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    margin-bottom: var(--spacing-4);
}

.stats-row {
    display: flex;
    justify-content: space-around;
    gap: var(--spacing-4);
}

.stat-item {
    text-align: center;
}

.stat-item .label {
    display: block;
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-2);
}

.stat-item .value {
    display: block;
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-primary);
}

/* 響應式：手機版垂直排列 */
@media (max-width: 768px) {
    .stats-row {
        flex-direction: column;
    }

    .stat-item {
        text-align: left;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: var(--spacing-2) 0;
        border-bottom: 1px solid var(--color-border);
    }

    .stat-item:last-child {
        border-bottom: none;
    }
}
```

**樣式重點:**
- 活動圖示使用圓形容器，不同類型不同背景色
- 活動項目使用 flexbox 排列，手機版垂直排列
- 商品概覽使用橫向排列，手機版改為列表樣式
  </action>
  <verify>
載入 Dashboard 頁面，檢查：
- 活動列表正確顯示
- 活動圖示樣式正確（訂單藍色，客戶綠色）
- 商品概覽橫向排列（桌面版）
- 手機版活動和商品概覽垂直排列
  </verify>
  <done>
活動列表和商品概覽樣式完成，響應式佈局正常
  </done>
</task>

<task type="auto">
  <name>Task 4: 定義載入狀態和錯誤狀態樣式</name>
  <files>buygo-plus-one-dev/admin/css/dashboard.css</files>
  <action>
在 `dashboard.css` 中加入載入和錯誤狀態樣式：

```css
/* 載入中骨架屏 */
.loading-skeleton {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

@keyframes pulse {
    0%, 100% {
        opacity: 1;
    }
    50% {
        opacity: 0.5;
    }
}

.skeleton-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--spacing-4);
}

.skeleton-line {
    background: var(--color-border);
    border-radius: var(--radius-sm);
    height: 16px;
    margin-bottom: var(--spacing-2);
}

.skeleton-line.short {
    width: 50%;
}

.skeleton-line.long {
    width: 75%;
}

.skeleton-line.title {
    height: 24px;
    width: 30%;
    margin-bottom: var(--spacing-3);
}

/* 錯誤訊息 */
.error-message {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid var(--color-danger);
    border-radius: var(--radius-xl);
    padding: var(--spacing-6);
    text-align: center;
}

.error-message .error-icon {
    width: 64px;
    height: 64px;
    margin: 0 auto var(--spacing-4);
    color: var(--color-danger);
}

.error-message .error-text {
    font-size: var(--font-size-base);
    color: var(--color-danger);
    margin-bottom: var(--spacing-4);
}

.error-message .retry-button {
    background: var(--color-primary);
    color: white;
    border: none;
    border-radius: var(--radius-lg);
    padding: var(--spacing-2) var(--spacing-4);
    font-size: var(--font-size-base);
    cursor: pointer;
    transition: all var(--transition-base);
}

.error-message .retry-button:hover {
    background: var(--color-primary-hover);
}
```

**樣式重點:**
- 骨架屏使用 pulse 動畫模擬載入狀態
- 錯誤訊息使用紅色系配色，清晰顯示問題
- 重試按鈕使用主色調，有 hover 效果
  </action>
  <verify>
模擬載入和錯誤狀態：
- 暫時延遲 API 回應，檢查骨架屏動畫
- 暫時讓 API 返回錯誤，檢查錯誤訊息樣式
  </verify>
  <done>
載入和錯誤狀態樣式完成，使用者體驗良好
  </done>
</task>

<task type="auto">
  <name>Task 5: 在 dashboard.php 中載入 dashboard.css</name>
  <files>buygo-plus-one-dev/admin/partials/dashboard.php</files>
  <action>
在 `dashboard.php` 的 `<head>` 區塊中載入 `dashboard.css`：

```php
<?php
// 載入 Dashboard 頁面專用樣式
?>
<link rel="stylesheet" href="<?php echo esc_url(plugins_url('../css/dashboard.css', __FILE__)); ?>">
```

確保 CSS 載入順序正確：
1. Design System tokens（colors, spacing, typography, effects）
2. Design System components（card, header, button）
3. Dashboard.css（頁面專用樣式）

參考其他頁面的 CSS 載入方式（`customers.php`, `products.php`）。
  </action>
  <verify>
檢查 CSS 載入：
```bash
# 檢查頁面原始碼包含 dashboard.css
curl http://buygo.me/buygo-portal/dashboard/ | grep dashboard.css
```

瀏覽器檢查：
- 開發者工具 > Network > 確認 dashboard.css 載入成功（200 OK）
- 檢查樣式是否生效（統計卡片有正確樣式）
  </verify>
  <done>
dashboard.css 已正確載入，所有樣式生效
  </done>
</task>

</tasks>

<verification>
**整體驗證:**

1. Dashboard 頁面所有區塊樣式正確
2. 響應式佈局正常（桌面/平板/手機）
3. 設計系統 tokens 正確使用
4. 載入和錯誤狀態樣式正常
5. 無 CSS 衝突或覆蓋問題

**測試檢查清單:**
- [ ] 統計卡片樣式正確（背景、邊框、陰影、hover）
- [ ] 變化百分比顏色正確（綠色/紅色）
- [ ] 圖表卡片樣式正確
- [ ] 圖表容器高度正確（桌面 300px，手機 250px）
- [ ] 活動列表樣式正確（圖示、內容、時間）
- [ ] 商品概覽樣式正確（橫向/垂直排列）
- [ ] 載入骨架屏動畫正常
- [ ] 錯誤訊息樣式正確
- [ ] 手機版佈局正常（所有區塊垂直排列）
- [ ] 所有樣式使用設計系統 tokens

**響應式測試:**
- [ ] 桌面版（> 768px）：4 欄統計卡片，2 欄圖表
- [ ] 平板版（768px）：2 欄統計卡片，1 欄圖表
- [ ] 手機版（< 768px）：1 欄統計卡片，1 欄圖表
</verification>

<success_criteria>
- [ ] dashboard.css 存在且語法正確
- [ ] 所有樣式使用設計系統 tokens
- [ ] 統計卡片、圖表、活動、商品概覽樣式完整
- [ ] 響應式佈局正確（3 個斷點）
- [ ] 載入和錯誤狀態樣式完整
- [ ] CSS 已在 dashboard.php 中載入
- [ ] 無 CSS 衝突或覆蓋問題
- [ ] 視覺效果符合設計系統規範
</success_criteria>

<output>
完成後建立 `.planning/phases/21-dashboard/21-04-SUMMARY.md` 記錄執行結果
</output>
