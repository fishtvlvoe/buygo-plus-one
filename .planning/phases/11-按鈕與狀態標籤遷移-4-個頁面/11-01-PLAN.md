---
phase: 11-按鈕與狀態標籤遷移-4-個頁面
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/components/button.css
  - design-system/components/status-tag.css
autonomous: true

must_haves:
  truths:
    - ".btn-danger class 存在於設計系統中"
    - ".btn-danger 有正確的紅色背景和 hover 狀態"
    - ".status-tag-danger 作為 .status-tag-error 的別名存在"
  artifacts:
    - path: "design-system/components/button.css"
      provides: ".btn-danger class definition"
      contains: ".btn-danger"
    - path: "design-system/components/status-tag.css"
      provides: ".status-tag-danger alias"
      contains: ".status-tag-danger"
  key_links:
    - from: "design-system/components/button.css"
      to: "design-system/tokens/colors.css"
      via: "CSS variables"
      pattern: "--color-error"
---

<objective>
補充設計系統中缺失的 `.btn-danger` 和 `.status-tag-danger` classes

Purpose: 確保後續頁面遷移時有完整的設計系統支援，避免刪除按鈕和錯誤狀態標籤樣式缺失
Output: 更新後的 button.css 和 status-tag.css
</objective>

<execution_context>
@/Users/fishtv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fishtv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@design-system/components/button.css
@design-system/components/status-tag.css
@design-system/tokens/colors.css
@.planning/phases/11-按鈕與狀態標籤遷移-4-個頁面/11-CONTEXT.md
@.planning/phases/11-按鈕與狀態標籤遷移-4-個頁面/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 .btn-danger class 到 button.css</name>
  <files>design-system/components/button.css</files>
  <action>
在 `.btn-secondary` 定義之後添加 `.btn-danger` class：

```css
/* Danger Button */
.btn-danger {
  background-color: var(--color-error);
  color: white;
}

.btn-danger:hover:not(:disabled) {
  background-color: var(--color-error-600);
}
```

確保使用現有的 CSS 變數 `--color-error` 和 `--color-error-600`。
  </action>
  <verify>
檢查 button.css 包含 `.btn-danger` class 定義：
```bash
grep -n "btn-danger" design-system/components/button.css
```
  </verify>
  <done>.btn-danger class 已添加到 button.css，包含背景色和 hover 狀態</done>
</task>

<task type="auto">
  <name>Task 2: 新增 .status-tag-danger alias 到 status-tag.css</name>
  <files>design-system/components/status-tag.css</files>
  <action>
在 `.status-tag-error` 定義之後添加 `.status-tag-danger` 作為別名，確保與 CONTEXT.md 中的命名一致：

```css
/* Danger Status (alias for error - for consistency with button naming) */
.status-tag-danger {
  background-color: var(--color-error-50);
  color: var(--color-error-600);
}
```

這樣可以同時支援 `.status-tag-error` 和 `.status-tag-danger` 兩種命名方式。
  </action>
  <verify>
檢查 status-tag.css 包含 `.status-tag-danger` class 定義：
```bash
grep -n "status-tag-danger" design-system/components/status-tag.css
```
  </verify>
  <done>.status-tag-danger class 已添加到 status-tag.css</done>
</task>

</tasks>

<verification>
1. 設計系統 CSS 檔案語法正確：
   ```bash
   # 確保沒有 CSS 語法錯誤（檢查大括號匹配）
   cat design-system/components/button.css | grep -c "{"
   cat design-system/components/button.css | grep -c "}"
   ```

2. 新增的 classes 使用正確的 CSS 變數：
   ```bash
   grep "color-error" design-system/components/button.css
   grep "color-error" design-system/components/status-tag.css
   ```
</verification>

<success_criteria>
- [ ] button.css 包含 `.btn-danger` class（紅色背景、白色文字）
- [ ] button.css 的 `.btn-danger:hover` 狀態使用 `--color-error-600`
- [ ] status-tag.css 包含 `.status-tag-danger` class
- [ ] 所有新增的 classes 使用現有的 CSS 變數
- [ ] CSS 檔案語法正確，無錯誤
</success_criteria>

<output>
After completion, create `.planning/phases/11-按鈕與狀態標籤遷移-4-個頁面/11-01-SUMMARY.md`
</output>
