---
phase: 57-route-and-quantity-selection
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - admin/partials/batch-create.php
  - includes/views/composables/useBatchCreate.js
  - includes/views/template.php
  - admin/js/components/BatchCreatePage.js
autonomous: true
requirements:
  - SELECT-01
  - SELECT-02
  - SELECT-03
  - SELECT-04

must_haves:
  truths:
    - "數量選擇頁顯示標題「要上架幾個商品？」和副標「選擇數量後，一次展開所有欄位填寫」"
    - "四個快選按鈕（5/10/15/20 個）可點擊，選中的以藍色實心顯示"
    - "自訂輸入框可輸入 1-20，輸入時自動取消快選按鈕選取狀態"
    - "頁面顯示剩餘配額資訊，選擇數量超過配額時「開始填寫」按鈕禁用"
    - "「開始填寫（N 個商品）→」按鈕動態顯示數量，點擊後可進入下一步"
    - "左上角返回箭頭「←」可回到商品列表頁"
  artifacts:
    - path: "includes/views/composables/useBatchCreate.js"
      provides: "批量上架的響應式狀態和邏輯"
      exports: ["useBatchCreate"]
    - path: "admin/partials/batch-create.php"
      provides: "數量選擇頁的 Vue template"
      contains: "batch-create-page-template"
    - path: "admin/js/components/BatchCreatePage.js"
      provides: "BatchCreatePageComponent 定義"
      contains: "BatchCreatePageComponent"
  key_links:
    - from: "includes/views/composables/useBatchCreate.js"
      to: "/wp-json/buygo-plus-one/v1/products/limit-check"
      via: "fetch 取得配額"
      pattern: "limit-check"
    - from: "admin/js/components/BatchCreatePage.js"
      to: "includes/views/composables/useBatchCreate.js"
      via: "setup() return useBatchCreate()"
      pattern: "useBatchCreate"
---

<objective>
實作批量上架的數量選擇頁 UI — 完整互動體驗。

Purpose: 賣家選擇要上架的商品數量（5/10/15/20 或自訂），系統即時顯示配額狀態，為 Phase 58 的表單填寫做準備。
Output: 完整可互動的數量選擇頁面，包含快選按鈕、自訂輸入、配額顯示、開始填寫按鈕
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-route-and-quantity-selection/57-01-SUMMARY.md

<!-- 設計圖參考 -->
@.planning/designs/批量上架 - 手機版-2.png

<!-- 現有程式碼參考 -->
@includes/views/composables/useApi.js
@includes/views/composables/useProducts.js
@admin/js/components/ProductsPage.js
@includes/views/template.php

<interfaces>
<!-- API 端點：配額查詢（已存在） -->
GET /wp-json/buygo-plus-one/v1/products/limit-check
Response: {
  success: true,
  data: {
    can_add: true,
    current: 12,   // 已使用
    limit: 50,     // 上限（0 = 無限制）
    message: "..."
  }
}

<!-- useApi composable（已存在，全域函式） -->
```javascript
function useApi() {
    return { isLoading, error, request, get, post, put, delete: del };
}
// get(url, options) — 自動帶 nonce 和防快取
```

<!-- BuyGoRouter（已存在 + Plan 01 擴充） -->
```javascript
BuyGoRouter.spaNavigate('products');  // 回到商品列表
BuyGoRouter.spaNavigate('batch-create');  // 當前頁面
```

<!-- 元件模式：thin shell + composable -->
```javascript
// ProductsPage.js 模式（參考）
const ProductsPageComponent = {
    name: 'ProductsPage',
    template: '#products-page-template',
    setup() { return useProducts(); }
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useBatchCreate composable — 數量選擇邏輯</name>
  <files>includes/views/composables/useBatchCreate.js, includes/views/template.php</files>
  <action>
**1. 建立 `includes/views/composables/useBatchCreate.js`：**

遵循現有 composable 模式（全域函式，不用 ES module）。使用 Vue 3 Composition API。

```javascript
function useBatchCreate() {
    const { ref, computed, onMounted } = Vue;
    const { get } = useApi();

    // ========== 步驟控制 ==========
    // 'select' = 數量選擇（Phase 57）
    // 'form' = 表單填寫（Phase 58 實作）
    const step = ref('select');

    // ========== 數量選擇狀態 ==========
    const selectedPreset = ref(null);  // 5, 10, 15, 20 或 null
    const customQuantity = ref('');     // 自訂輸入值
    const presetOptions = [5, 10, 15, 20];

    // 當前選擇的數量
    const quantity = computed(() => {
        if (selectedPreset.value !== null) return selectedPreset.value;
        const custom = parseInt(customQuantity.value);
        if (!isNaN(custom) && custom >= 1 && custom <= 20) return custom;
        return 0;
    });

    // 選擇快選按鈕
    const selectPreset = (num) => {
        selectedPreset.value = num;
        customQuantity.value = '';  // 清空自訂輸入
    };

    // 自訂輸入時取消快選
    const onCustomInput = () => {
        selectedPreset.value = null;
    };

    // ========== 配額狀態 ==========
    const quota = ref({ can_add: true, current: 0, limit: 0, message: '' });
    const quotaLoading = ref(true);

    // 剩餘配額
    const remaining = computed(() => {
        if (quota.value.limit === 0) return Infinity;  // 無限制
        return Math.max(0, quota.value.limit - quota.value.current);
    });

    // 是否超過配額
    const isOverQuota = computed(() => {
        if (quota.value.limit === 0) return false;  // 無限制
        return quantity.value > remaining.value;
    });

    // 「開始填寫」按鈕是否可用
    const canProceed = computed(() => {
        return quantity.value > 0 && !isOverQuota.value;
    });

    // 載入配額資訊
    const loadQuota = async () => {
        quotaLoading.value = true;
        try {
            const res = await get('/wp-json/buygo-plus-one/v1/products/limit-check', {
                showError: false
            });
            if (res && res.data) {
                quota.value = res.data;
            }
        } catch (e) {
            console.error('載入配額失敗:', e);
        } finally {
            quotaLoading.value = false;
        }
    };

    // ========== 導航 ==========
    const goBack = () => {
        if (window.BuyGoRouter) {
            window.BuyGoRouter.spaNavigate('products');
        }
    };

    const startFilling = () => {
        if (!canProceed.value) return;
        // Phase 58 會實作：step.value = 'form';
        // 暫時先 console.log 提示
        console.log('開始填寫', quantity.value, '個商品');
    };

    // ========== 生命週期 ==========
    onMounted(() => {
        loadQuota();
    });

    return {
        // 步驟
        step,
        // 數量選擇
        selectedPreset, customQuantity, presetOptions,
        quantity, selectPreset, onCustomInput,
        // 配額
        quota, quotaLoading, remaining, isOverQuota, canProceed,
        loadQuota,
        // 導航
        goBack, startFilling
    };
}
```

**2. 在 template.php 中載入 composable：**

在「頁面 Composables」區塊（`useShipmentDetails.js` 之後）加入：
```php
<script><?php include BUYGO_PLUS_ONE_PLUGIN_DIR . 'includes/views/composables/useBatchCreate.js'; ?></script>
```

**重要設計決策：**
- `quota.limit === 0` 表示無限制（真實賣家），此時 remaining 為 Infinity，永不超額
- `customQuantity` 存為字串，避免 number input 的 `0` 預設值問題
- 預設不選中任何快選按鈕（selectedPreset = null），需要用戶主動選擇
  </action>
  <verify>
    <automated>cd /Users/fishtv/Development/wp-plugins/buygo-plus-one && test -f includes/views/composables/useBatchCreate.js && grep "useBatchCreate" includes/views/composables/useBatchCreate.js && grep "useBatchCreate" includes/views/template.php && echo "PASS"</automated>
  </verify>
  <done>
    - useBatchCreate.js 存在，定義全域函式 useBatchCreate()
    - template.php 載入此 composable
    - 包含 quantity/selectedPreset/customQuantity 響應式狀態
    - 包含 quota 配額狀態和 loadQuota() 呼叫 limit-check API
    - 包含 remaining/isOverQuota/canProceed 計算屬性
    - 包含 goBack/startFilling 導航方法
  </done>
</task>

<task type="auto">
  <name>Task 2: 數量選擇頁 UI（模板 + 元件）</name>
  <files>admin/partials/batch-create.php, admin/js/components/BatchCreatePage.js</files>
  <action>
**1. 建立 `admin/js/components/BatchCreatePage.js`：**

遵循 ProductsPage.js 的 thin shell 模式：

```javascript
const BatchCreatePageComponent = {
    name: 'BatchCreatePage',
    template: '#batch-create-page-template',
    setup() {
        return useBatchCreate();
    }
};
```

**2. 重寫 `admin/partials/batch-create.php`（替換 Plan 01 的空殼）：**

完整實作數量選擇頁面。結構：

```php
<?php // 批量上架頁面元件 ?>
<!-- 批量上架 CSS（行內，繞過 WAF） -->
<style>
/* 批量上架專用樣式 */
.batch-preset-btn {
    width: 72px;
    height: 72px;
    border-radius: 12px;
    border: 2px solid #e2e8f0;
    background: white;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s;
    font-weight: 700;
}
.batch-preset-btn:hover {
    border-color: #2563EB;
    background: #f0f5ff;
}
.batch-preset-btn.active {
    background: #2563EB;
    border-color: #2563EB;
    color: white;
}
.batch-preset-btn .number {
    font-size: 24px;
    line-height: 1;
}
.batch-preset-btn .unit {
    font-size: 12px;
    margin-top: 2px;
    opacity: 0.8;
}
</style>

<script type="text/x-template" id="batch-create-page-template">
<!-- 模板最外層 -->
<div class="min-h-screen bg-slate-50 text-slate-900 font-sans antialiased">
  <main class="flex flex-col min-w-0 relative bg-slate-50 min-h-screen">

    <!-- 頂部導航列 -->
    <div class="sticky top-0 z-10 bg-white border-b border-slate-200 px-4 py-3 flex items-center gap-3">
      <button @click="goBack" class="p-1 rounded-lg hover:bg-slate-100 transition">
        <svg class="w-5 h-5 text-slate-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
      </button>
      <h1 class="text-lg font-bold text-slate-800">批量上架</h1>
    </div>

    <!-- 步驟 1: 數量選擇 (SELECT-01 ~ SELECT-04) -->
    <div v-if="step === 'select'" class="flex-1 flex flex-col">
      <div class="flex-1 px-4 py-8 md:py-12">
        <div class="max-w-sm mx-auto">

          <!-- 圖示 -->
          <div class="flex justify-center mb-6">
            <div class="w-16 h-16 rounded-full bg-blue-50 flex items-center justify-center">
              <svg class="w-8 h-8 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
              </svg>
            </div>
          </div>

          <!-- 標題 (SELECT-01) -->
          <h2 class="text-xl font-bold text-slate-900 text-center mb-1">要上架幾個商品？</h2>
          <p class="text-sm text-slate-500 text-center mb-8">選擇數量後，一次展開所有欄位填寫</p>

          <!-- 快選按鈕 (SELECT-01) -->
          <div class="flex justify-center gap-3 mb-6">
            <button v-for="num in presetOptions" :key="num"
              @click="selectPreset(num)"
              class="batch-preset-btn"
              :class="{ active: selectedPreset === num }">
              <span class="number">{{ num }}</span>
              <span class="unit">個</span>
            </button>
          </div>

          <!-- 分隔線 + "或" (SELECT-02) -->
          <div class="flex items-center gap-3 mb-6">
            <div class="flex-1 border-t border-slate-200"></div>
            <span class="text-sm text-slate-400">或</span>
            <div class="flex-1 border-t border-slate-200"></div>
          </div>

          <!-- 自訂數量輸入 (SELECT-02) -->
          <div class="relative mb-6">
            <span class="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 text-lg font-bold">#</span>
            <input type="number"
              v-model="customQuantity"
              @input="onCustomInput"
              min="1" max="20"
              placeholder="自訂數量 (1-20)"
              class="w-full pl-10 pr-4 py-3 border border-slate-300 rounded-xl text-base focus:border-primary focus:ring-2 focus:ring-blue-100 outline-none transition">
          </div>

          <!-- 配額資訊 (SELECT-03) -->
          <div v-if="!quotaLoading && quota.limit > 0"
            class="flex items-center gap-2 px-4 py-3 rounded-xl mb-6"
            :class="isOverQuota ? 'bg-red-50 text-red-600' : 'bg-blue-50 text-blue-600'">
            <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span class="text-sm">
              剩餘配額：{{ remaining }} 個（已用 {{ quota.current }} / 上限 {{ quota.limit }}）
            </span>
          </div>
          <!-- 無限制賣家提示 -->
          <div v-if="!quotaLoading && quota.limit === 0"
            class="flex items-center gap-2 px-4 py-3 rounded-xl bg-green-50 text-green-600 mb-6">
            <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span class="text-sm">無商品數量限制</span>
          </div>
          <!-- 配額載入中 -->
          <div v-if="quotaLoading"
            class="flex items-center gap-2 px-4 py-3 rounded-xl bg-slate-100 text-slate-400 mb-6">
            <span class="text-sm">載入配額中...</span>
          </div>

        </div>
      </div>

      <!-- 底部按鈕 (SELECT-04) -->
      <div class="sticky bottom-0 bg-white border-t border-slate-200 p-4">
        <div class="max-w-sm mx-auto">
          <button @click="startFilling"
            :disabled="!canProceed"
            class="w-full py-3.5 rounded-xl font-bold text-base transition flex items-center justify-center gap-2"
            :class="canProceed
              ? 'bg-primary text-white hover:bg-blue-700 shadow-lg shadow-blue-200'
              : 'bg-slate-200 text-slate-400 cursor-not-allowed'">
            <span v-if="quantity > 0">開始填寫（{{ quantity }} 個商品）</span>
            <span v-else>請選擇數量</span>
            <svg v-if="canProceed" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <!-- 步驟 2: 表單填寫（Phase 58 實作） -->
    <div v-if="step === 'form'" class="flex-1 flex items-center justify-center p-4">
      <div class="text-center text-slate-500">
        <p class="text-lg font-bold mb-2">表單填寫</p>
        <p>Phase 58 實作</p>
      </div>
    </div>

  </main>
</div>
</script>

<?php // 載入元件 JS ?>
<script><?php include plugin_dir_path(dirname(__FILE__)) . 'js/components/BatchCreatePage.js'; ?></script>
```

**對照設計圖的關鍵 UI 細節：**
- 頂部導航：白底 + 下邊框 + 返回箭頭 + "批量上架" 標題（居左）
- 圖示：藍色圓形背景 + 盒子圖示（設計圖用 layers 圖示，這裡用 package/box 圖示近似）
- 快選按鈕：4 個方形按鈕，選中時藍色實心 + 白色文字
- 分隔：「或」字居中，左右各一條分隔線
- 自訂輸入：# 前綴圖示 + placeholder "自訂數量 (1-20)"
- 配額：ℹ️ 圖示 + 藍色背景條（超額時紅色）
- CTA 按鈕：全寬藍色，底部 sticky，動態文字 "開始填寫（N 個商品）→"

**注意：** CSS 使用 inline style（`<style>` 標籤在 partial 中），這是此專案的標準做法（繞過 InstaWP WAF）。
  </action>
  <verify>
    <automated>cd /Users/fishtv/Development/wp-plugins/buygo-plus-one && test -f admin/js/components/BatchCreatePage.js && grep "BatchCreatePageComponent" admin/js/components/BatchCreatePage.js && grep "batch-create-page-template" admin/partials/batch-create.php && grep "selectPreset" admin/partials/batch-create.php && grep "startFilling" admin/partials/batch-create.php && echo "PASS"</automated>
  </verify>
  <done>
    - BatchCreatePage.js 存在，定義 thin shell 元件
    - batch-create.php 包含完整數量選擇 UI 模板
    - 四個快選按鈕（5/10/15/20）可點擊且有 active 狀態
    - 自訂輸入框有 # 前綴、placeholder、輸入時取消快選
    - 配額資訊從 API 載入並顯示（含超額紅色提示）
    - 「開始填寫」按鈕動態顯示數量、超額時禁用
    - 返回箭頭可 SPA 導航回商品列表
  </done>
</task>

</tasks>

<verification>
1. 瀏覽器存取 /buygo-portal/batch-create/ — 顯示數量選擇頁面
2. 點擊 5/10/15/20 按鈕 — 選中的按鈕變藍，其他恢復白色
3. 在自訂輸入框輸入 8 — 快選按鈕全部取消選取
4. 配額資訊區域顯示正確的「剩餘配額：N 個（已用 X / 上限 Y）」
5. 選擇超過配額的數量 — 「開始填寫」按鈕變灰色禁用
6. 點擊左上角 ← — 回到商品列表頁（SPA 導航）
</verification>

<success_criteria>
- 數量選擇頁完整可互動，UI 對照設計圖一致
- 快選按鈕和自訂輸入互斥（切換時自動清除對方狀態）
- 配額資訊正確從 API 載入，超額時 CTA 按鈕禁用
- SPA 導航正常（返回箭頭、瀏覽器直接存取路徑）
</success_criteria>

<output>
After completion, create `.planning/phases/57-route-and-quantity-selection/57-02-SUMMARY.md`
</output>
