---
phase: 49-product-custom-fields
plan: 01
type: execute
wave: 1
files_modified:
  - includes/database/class-database.php (or new file)
  - includes/api/class-reserved-api.php
  - includes/services/class-product-service.php
  - includes/views/composables/useProducts.js
  - admin/partials/products.php
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03]
---

<objective>
商品詳情頁新增「成本與來源」Tab，包含 6 個自訂欄位、REST API、利潤即時計算。

Output: 商品編輯頁雙 Tab（基本資訊 | 成本與來源），自訂欄位可 CRUD，利潤即時顯示。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: buygo_product_meta 資料表 + ProductMetaService</name>
  <files>
    includes/database/class-product-meta-table.php (新建)
    includes/services/class-product-meta-service.php (新建)
    includes/class-plugin.php
  </files>
  <action>
**1. 建立 buygo_product_meta 資料表：**

```sql
CREATE TABLE {prefix}buygo_product_meta (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    product_id BIGINT UNSIGNED NOT NULL,
    meta_key VARCHAR(255) NOT NULL,
    meta_value LONGTEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY product_meta (product_id, meta_key),
    KEY product_id (product_id)
) {charset_collate};
```

在 class-plugin.php 的 activate() 或 init 中呼叫建表。

**2. 建立 ProductMetaService：**

```php
class ProductMetaService {
    public static function get_fields($product_id): array
    public static function update_fields($product_id, array $fields): bool
    // 支援的 meta_key：cost_price, original_price, purchase_location, supplier, barcode, manufacturing_notes
}
```
  </action>
  <verify>
  1. 啟用外掛後 buygo_product_meta 表存在
  2. ProductMetaService::get_fields() 回傳空陣列（新商品）
  3. ProductMetaService::update_fields() 寫入後可讀取
  </verify>
  <done>buygo_product_meta 表建立 + ProductMetaService CRUD 就緒</done>
</task>

<task type="auto">
  <name>Task 2: Custom Fields REST API（替換 501 骨架）</name>
  <files>
    includes/api/class-reserved-api.php
  </files>
  <action>
將 class-reserved-api.php 中的 custom-fields 端點從 501 改為實際實作：

**GET /products/{id}/custom-fields：**
- 呼叫 ProductMetaService::get_fields($product_id)
- 回傳 6 個欄位的值（空欄位回傳空字串）
- 權限：check_permission()（與商品 API 一致）

**PUT /products/{id}/custom-fields：**
- 接收 JSON body 含要更新的欄位
- 呼叫 ProductMetaService::update_fields($product_id, $fields)
- 驗證 meta_key 在白名單內
- 權限：check_permission()
  </action>
  <verify>
  1. GET /products/1/custom-fields 回傳 200（不再是 501）
  2. PUT /products/1/custom-fields 寫入後 GET 可讀取
  3. 無權限使用者收到 403
  </verify>
  <done>Custom Fields API 完整實作，替換 Phase 45 骨架</done>
</task>

<task type="auto">
  <name>Task 3: 商品編輯頁雙 Tab UI + 利潤計算</name>
  <files>
    includes/views/composables/useProducts.js
    admin/partials/products.php
  </files>
  <action>
**1. useProducts.js 新增自訂欄位邏輯：**

- 新增 ref：`customFields`（6 個欄位）、`customFieldsLoading`、`customFieldsSaving`
- 新增方法：`loadCustomFields(productId)`、`saveCustomFields()`
- 新增 computed：`profit`（售價 - 成本價）、`profitMargin`（利潤率 %）
- 在 navigateTo('edit', id) 時同時載入 custom fields

**2. products.php 模板改動：**

現有編輯表單（第 685-751 行）改為 Tab 結構：

```html
<!-- Tab 切換按鈕 -->
<div class="flex border-b border-slate-200 mb-4">
    <button @click="editTab = 'basic'" :class="editTab === 'basic' ? 'active' : ''">基本資訊</button>
    <button @click="editTab = 'cost'" :class="editTab === 'cost' ? 'active' : ''">成本與來源</button>
</div>

<!-- Tab 1: 基本資訊（現有內容不變） -->
<div v-show="editTab === 'basic'">
    <!-- 現有編輯表單 -->
</div>

<!-- Tab 2: 成本與來源 -->
<div v-show="editTab === 'cost'">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <!-- 成本價 -->
        <!-- 原價 -->
        <!-- 購買地 -->
        <!-- 供應商 -->
        <!-- 條碼 -->
        <!-- 製造內容（textarea） -->
    </div>
    <!-- 利潤顯示（即時計算） -->
    <div v-if="customFields.cost_price" class="mt-4 p-3 bg-green-50 rounded-lg">
        利潤：{{ profit }} | 利潤率：{{ profitMargin }}%
    </div>
    <!-- 儲存按鈕 -->
</div>
```

**3. 利潤計算（前端即時）：**
- 利潤 = editingProduct.price - customFields.cost_price
- 利潤率 = (利潤 / editingProduct.price) × 100
- 無成本價時不顯示
  </action>
  <verify>
  1. 商品編輯頁有「基本資訊」和「成本與來源」兩個 Tab
  2. 成本與來源 Tab 有 6 個欄位可填寫
  3. 填入成本價後即時顯示利潤和利潤率
  4. 儲存後重新進入，資料正確載入
  5. 基本資訊 Tab 功能不受影響
  </verify>
  <done>商品編輯頁雙 Tab + 6 欄位 + 利潤即時計算完成</done>
</task>

</tasks>

<verification>
1. 商品編輯頁有兩個 Tab
2. 6 個自訂欄位可填寫、儲存、重新載入
3. 利潤即時計算正確
4. API GET/PUT 正常運作
5. 權限檢查通過
</verification>

<success_criteria>
- buygo_product_meta 表正確建立
- GET/PUT /products/{id}/custom-fields API 完整實作
- 商品編輯頁雙 Tab UI
- 利潤前端即時計算
</success_criteria>
