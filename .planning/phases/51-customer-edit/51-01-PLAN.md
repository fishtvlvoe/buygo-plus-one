---
phase: 51-customer-edit
plan: 01
type: execute
wave: 1
files_modified:
  - admin/js/components/CustomersPage.js
  - admin/partials/customers.php
  - includes/api/class-customers-api.php
  - includes/services/class-customer-service.php (新建或擴充)
autonomous: true
requirements: [CUST-01, CUST-02, CUST-03, CUST-04, CUST-05]
---

<objective>
客戶詳情頁 Tab 順序對調 + inline 全欄位編輯模式 + FluentCart 同步。

Output: 客戶資訊 Tab 為預設，點「編輯」可 inline 編輯所有欄位，儲存同步 FluentCart。
</objective>

<context>
已有的基礎設施：
- CustomersPage.js 有 detail view（currentView === 'detail'）
- customers.php 有客戶資訊 Tab（第 332-406 行），目前全唯讀（只有備註可編輯）
- Data Management API 已有 PUT /data-management/customers/{id}（管理員專屬）
- FluentCart 表：fct_customers + fct_customer_addresses
- 自訂欄位存 wp_usermeta（buygo_taiwan_id_number）

策略：
- 不沿用 DataManagement API（權限太嚴格，只有管理員）
- 在 Customers API 新增 PUT /customers/{id} 端點，權限用 check_permission_with_scope('customers')
- 前端改 CustomersPage.js + customers.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: 客戶編輯 REST API + Service</name>
  <files>
    includes/api/class-customers-api.php
    includes/services/class-customer-edit-service.php (新建)
  </files>
  <action>
**1. 新增 PUT /customers/{id} 端點：**

在 class-customers-api.php 中註冊：
```php
register_rest_route('buygo-plus-one/v1', '/customers/(?P<id>\d+)', [
    'methods' => 'PUT',
    'callback' => [$this, 'update_customer'],
    'permission_callback' => [API::class, 'check_permission_with_scope_customers'],
]);
```

**2. 建立 CustomerEditService：**

```php
class CustomerEditService {
    public static function update($customer_id, array $data): array {
        // 1. 驗證客戶屬於當前賣場
        // 2. 更新 fct_customers（first_name, last_name）
        // 3. 更新 fct_customer_addresses（phone, address_1, address_2, city, state, postcode, country）
        // 4. 更新 wp_usermeta（buygo_taiwan_id_number, buygo_custom_id）
        // 5. 回傳更新後的客戶資料
    }
}
```

**3. 跨賣場存取檢查：**
- 透過客戶的訂單紀錄確認此客戶屬於當前賣家
- 非自己的客戶回傳 403

**4. 可編輯欄位白名單：**
- first_name, last_name（fct_customers）
- phone, address_1, address_2, city, state, postcode, country（fct_customer_addresses）
- taiwan_id_number, custom_id（wp_usermeta / buygo_customer_meta）
- email 不可編輯（唯讀）
  </action>
  <verify>
  1. PUT /customers/1 可更新姓名
  2. 更新後 fct_customers 和 fct_customer_addresses 資料一致
  3. 無權限使用者收到 403
  4. 跨賣場存取收到 403
  </verify>
  <done>客戶編輯 API + Service 完成，含權限和 FluentCart 同步</done>
</task>

<task type="auto">
  <name>Task 2: 客戶詳情頁 Tab 對調 + Inline 編輯 UI</name>
  <files>
    admin/js/components/CustomersPage.js
    admin/partials/customers.php
  </files>
  <action>
**1. Tab 順序對調（CUST-01）：**

customers.php 中，把「客戶資訊」Tab 改為預設（activeDetailTab 預設值改為 'info'），Tab 按鈕順序對調。

CustomersPage.js 中：
```javascript
const activeDetailTab = ref('info'); // 改為 'info'（現在是 'orders'）
```

**2. 編輯模式切換（CUST-02）：**

CustomersPage.js 新增：
```javascript
const isEditing = ref(false);
const editForm = ref({});
const saving = ref(false);

const startEdit = () => {
    editForm.value = {
        first_name: selectedCustomer.value.first_name || '',
        last_name: selectedCustomer.value.last_name || '',
        phone: selectedCustomer.value.phone || '',
        address_1: selectedCustomer.value.address_1 || '',
        address_2: selectedCustomer.value.address_2 || '',
        city: selectedCustomer.value.city || '',
        state: selectedCustomer.value.state || '',
        postcode: selectedCustomer.value.postcode || '',
        country: selectedCustomer.value.country || '',
        taiwan_id_number: selectedCustomer.value.taiwan_id_number || '',
        custom_id: selectedCustomer.value.custom_id || '',
    };
    isEditing.value = true;
};

const cancelEdit = () => { isEditing.value = false; };

const saveEdit = async () => {
    saving.value = true;
    try {
        const response = await fetch(`/wp-json/buygo-plus-one/v1/customers/${selectedCustomer.value.id}`, {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json', 'X-WP-Nonce': wpNonce },
            body: JSON.stringify(editForm.value)
        });
        const result = await response.json();
        if (result.success) {
            // 更新本地資料
            Object.assign(selectedCustomer.value, editForm.value);
            isEditing.value = false;
            // Toast 成功
        }
    } finally { saving.value = false; }
};
```

**3. customers.php 模板改動：**

客戶資訊 Tab 中，每個欄位改為條件渲染：
```html
<!-- 姓名 -->
<div v-if="!isEditing">{{ selectedCustomer.first_name }} {{ selectedCustomer.last_name }}</div>
<div v-else class="flex gap-2">
    <input v-model="editForm.first_name" placeholder="姓" class="..." />
    <input v-model="editForm.last_name" placeholder="名" class="..." />
</div>
```

右上角加「編輯」/「儲存」/「取消」按鈕：
```html
<div class="flex gap-2">
    <button v-if="!isEditing" @click="startEdit">編輯</button>
    <template v-else>
        <button @click="cancelEdit">取消</button>
        <button @click="saveEdit" :disabled="saving">儲存</button>
    </template>
</div>
```

Email 欄位始終唯讀（不受 isEditing 影響）。

**4. 新增自訂欄位顯示（CUST-03）：**
- 身分證字號：已有顯示，改為可編輯
- 自訂編號：新增欄位顯示 + 可編輯
  </action>
  <verify>
  1. 進入客戶詳情頁，預設顯示「客戶資訊」Tab
  2. 客戶資訊 Tab 有「編輯」按鈕
  3. 點「編輯」後欄位可填寫，Email 維持唯讀
  4. 點「儲存」成功更新，自動退出編輯模式
  5. 點「取消」恢復原值
  6. 重新進入詳情頁，資料已更新
  </verify>
  <done>客戶詳情頁 Tab 對調 + inline 編輯模式完成</done>
</task>

</tasks>

<verification>
1. 客戶資訊 Tab 為預設
2. 編輯模式可切換
3. 所有欄位可編輯（Email 唯讀）
4. 儲存同步 FluentCart
5. 跨賣場存取被拒
</verification>

<success_criteria>
- Tab 順序：客戶資訊 | 訂單紀錄
- Inline 編輯：姓名、電話、地址、身分證、自訂編號
- API：PUT /customers/{id} 含權限檢查
- FluentCart 同步：fct_customers + fct_customer_addresses
</success_criteria>
