---
phase: 58-batch-form-and-csv
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - includes/views/composables/useBatchCreate.js
  - admin/partials/batch-create.php
autonomous: true
requirements:
  - FORM-05
  - CSV-01
  - CSV-02
  - CSV-03

must_haves:
  truths:
    - "手機版有「手動輸入」/「CSV 匯入」pill 按鈕可切換模式"
    - "桌面版有「匯入 CSV」按鈕（在配額 badge 旁邊）"
    - "切換到 CSV 模式時保留已填寫的手動資料"
    - "CSV 上傳區支援點擊選擇檔案和拖放上傳，拖放時上傳區邊框變藍提供視覺回饋"
    - "CSV 上傳後正確解析名稱/售價/數量/描述欄位，數量缺失時預設 0"
    - "CSV 格式錯誤時顯示錯誤提示"
    - "解析成功後填入表單並顯示匯入筆數提示，使用者可在表單中繼續修改"
  artifacts:
    - path: "includes/views/composables/useBatchCreate.js"
      provides: "CSV 解析邏輯 + 模式切換狀態"
      exports: ["useBatchCreate"]
    - path: "admin/partials/batch-create.php"
      provides: "手機版 pill 切換 + 桌面版 CSV 按鈕 + CSV 上傳 UI + 匯入結果提示"
      contains: "bp-csv-upload"
  key_links:
    - from: "includes/views/composables/useBatchCreate.js"
      to: "items ref"
      via: "importCsv() 解析 CSV 後填入 items 陣列"
      pattern: "importCsv|parseCSV"
    - from: "admin/partials/batch-create.php"
      to: "includes/views/composables/useBatchCreate.js"
      via: "Vue template 綁定 formMode/handleCsvUpload/handleDrop/isDragging/csvError/csvSuccess"
      pattern: "formMode|handleCsvUpload|handleDrop"
---

<objective>
實作 CSV 匯入功能和手動/CSV 模式切換 UI — 賣家可上傳 CSV 檔批次填入商品資料。

Purpose: 讓有大量商品的賣家能透過 CSV 快速匯入，不需逐一手動填寫。解析完全在前端完成，數量缺失時預設 0（無限），格式錯誤有明確提示。
Output: CSV 解析邏輯 + 模式切換 UI + 上傳區 + 匯入結果提示
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-batch-form-and-csv/58-01-PLAN.md

<!-- 設計圖參考 -->
@.planning/designs/批量上架 - 手機版.png (手機版：pill 按鈕「手動輸入」/「CSV 匯入」在配額進度條下方)
@.planning/designs/批量上架 - 手機版-1.png (桌面版：右上角「匯入 CSV」按鈕)

<!-- 現有程式碼（Plan 01 產出後） -->
@includes/views/composables/useBatchCreate.js
@admin/partials/batch-create.php

<interfaces>
<!-- Plan 01 新增的表單狀態（將由 58-01-SUMMARY.md 確認） -->
```javascript
// useBatchCreate composable — Plan 01 擴充後
function useBatchCreate() {
    // ...Phase 57 狀態...

    // 表單狀態（Plan 01）
    const items = ref([]);                    // 商品陣列 { id, name, price, quantity, description }
    let nextId = 1;
    const createEmptyItem = () => ({...});
    const initItems = (count) => {...};
    const addItem = () => {...};
    const removeItem = (id) => {...};
    const itemCount = computed(...)           // items.length
    const quotaUsed = computed(...)           // current + itemCount
    const quotaPercent = computed(...)        // 進度百分比
    const isFormOverQuota = computed(...)     // 表單超額

    return {
        // ...所有狀態...
        items, createEmptyItem, initItems, addItem, removeItem,
        itemCount, quotaUsed, quotaPercent, isFormOverQuota,
    };
}
```

<!-- batch-create.php Plan 01 結構 -->
```
<style>
  .batch-preset-btn { ... }     <!-- Phase 57 -->
  .bp-quota-bar { ... }         <!-- Plan 01 -->
  .bp-card { ... }              <!-- Plan 01 -->
  .bp-table { ... }             <!-- Plan 01 -->
  .bp-mobile-only / .bp-desktop-only
</style>
<script type="text/x-template" id="batch-create-page-template">
  <div>
    <nav>返回 + 標題</nav>
    <div v-if="step === 'select'">        <!-- 數量選擇 -->
    <div v-if="step === 'form'">          <!-- 表單步驟 -->
      <div class="bp-desktop-only">       <!-- 桌面版配額 badge -->
      <div class="bp-mobile-only">        <!-- 手機版配額進度條 -->
      <div>                               <!-- 表單主體 -->
        <div class="bp-mobile-only">      <!-- 手機版卡片 -->
        <div class="bp-desktop-only">     <!-- 桌面版表格 -->
      </div>
    </div>
  </div>
</script>
```

<!-- CSV 格式規格 -->
CSV 欄位對應：
- 第一行為表頭（名稱/name, 售價/price, 數量/quantity, 描述/description）
- 欄位名支援中英文（名稱|name, 售價|price, 數量|quantity, 描述|description）
- 數量缺失或非數字 → 預設 0（無限上架）
- 必填檢查：名稱和售價必須有值
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useBatchCreate composable 擴充 — CSV 解析 + 模式切換</name>
  <files>includes/views/composables/useBatchCreate.js</files>
  <action>
在 useBatchCreate.js 的表單狀態區塊（Plan 01 新增的 `// ========== 表單狀態（Phase 58）==========` 區塊）之後，新增 CSV 相關邏輯。

**1. 在 isFormOverQuota 之後新增 CSV/模式切換區塊：**

```javascript
// ========== 模式切換 + CSV 匯入（Phase 58 Plan 02） ==========

/**
 * 表單模式：'manual' = 手動輸入, 'csv' = CSV 匯入
 * 切換時保留已填寫的手動資料
 */
const formMode = ref('manual');

/**
 * CSV 匯入相關狀態
 */
const csvError = ref('');           // CSV 解析錯誤訊息
const csvSuccessMsg = ref('');      // CSV 匯入成功提示（例如「成功匯入 8 筆」）
const csvUploading = ref(false);    // 上傳中狀態

/**
 * 切換表單模式
 * 切換到 csv 模式時保留 items（不清空）
 */
const setFormMode = (mode) => {
    formMode.value = mode;
    csvError.value = '';
    csvSuccessMsg.value = '';
};

/**
 * 解析 CSV 文字內容
 * 支援中英文表頭，自動對應欄位
 * @param {string} text - CSV 原始文字
 * @returns {{ data: Array, error: string }}
 */
const parseCSV = (text) => {
    const lines = text.trim().split(/\r?\n/);
    if (lines.length < 2) {
        return { data: [], error: 'CSV 至少需要表頭和一行資料' };
    }

    // 解析表頭 — 支援中英文欄位名
    const headerLine = lines[0];
    const headers = headerLine.split(',').map(h => h.trim().replace(/^["']|["']$/g, '').toLowerCase());

    // 欄位對照表
    const fieldMap = {};
    const nameAliases = ['名稱', 'name', '商品名稱', '品名'];
    const priceAliases = ['售價', 'price', '價格', '單價'];
    const qtyAliases = ['數量', 'quantity', 'qty', '庫存'];
    const descAliases = ['描述', 'description', 'desc', '說明'];

    headers.forEach((h, i) => {
        if (nameAliases.includes(h)) fieldMap.name = i;
        else if (priceAliases.includes(h)) fieldMap.price = i;
        else if (qtyAliases.includes(h)) fieldMap.quantity = i;
        else if (descAliases.includes(h)) fieldMap.description = i;
    });

    // 驗證必要欄位
    if (fieldMap.name === undefined) {
        return { data: [], error: 'CSV 缺少「名稱」欄位（支援：名稱、name、商品名稱、品名）' };
    }
    if (fieldMap.price === undefined) {
        return { data: [], error: 'CSV 缺少「售價」欄位（支援：售價、price、價格、單價）' };
    }

    // 解析資料行
    const data = [];
    const errors = [];

    for (let i = 1; i < lines.length; i++) {
        const line = lines[i].trim();
        if (!line) continue;  // 跳過空行

        const cols = line.split(',').map(c => c.trim().replace(/^["']|["']$/g, ''));

        const name = cols[fieldMap.name] || '';
        const price = cols[fieldMap.price] || '';
        const qty = fieldMap.quantity !== undefined ? cols[fieldMap.quantity] : '';
        const desc = fieldMap.description !== undefined ? cols[fieldMap.description] : '';

        // 驗證每行必填
        if (!name) {
            errors.push('第 ' + (i + 1) + ' 行缺少商品名稱');
            continue;
        }
        if (!price || isNaN(Number(price))) {
            errors.push('第 ' + (i + 1) + ' 行售價無效');
            continue;
        }

        // 數量缺失或非數字 → 預設 0（無限）
        const parsedQty = qty && !isNaN(Number(qty)) ? String(Math.max(0, Math.floor(Number(qty)))) : '0';

        data.push({
            name: name,
            price: String(price),
            quantity: parsedQty,
            description: desc
        });
    }

    if (data.length === 0) {
        const errMsg = errors.length > 0
            ? '無有效資料。' + errors.join('；')
            : '無有效資料行';
        return { data: [], error: errMsg };
    }

    return {
        data: data,
        error: errors.length > 0 ? '部分行有錯誤（已跳過）：' + errors.join('；') : ''
    };
};

/**
 * 處理 CSV 檔案上傳
 * 前端解析 CSV → 填入 items 陣列
 * @param {Event} event - file input change event
 */
const handleCsvUpload = (event) => {
    const file = event.target.files && event.target.files[0];
    if (!file) return;

    csvError.value = '';
    csvSuccessMsg.value = '';
    csvUploading.value = true;

    const reader = new FileReader();
    reader.onload = (e) => {
        try {
            const text = e.target.result;
            const result = parseCSV(text);

            if (result.error && result.data.length === 0) {
                // 完全失敗
                csvError.value = result.error;
                csvUploading.value = false;
                return;
            }

            // 將解析結果填入 items
            // 策略：替換現有空白項 + 追加新項
            const newItems = result.data.map(d => ({
                id: nextId++,
                name: d.name,
                price: d.price,
                quantity: d.quantity,
                description: d.description
            }));

            // 保留已有手動填寫資料的 items，只替換空白的
            const filledItems = items.value.filter(item =>
                item.name.trim() !== '' || item.price.trim() !== ''
            );

            items.value = [...filledItems, ...newItems];

            // 如果合併後沒有項目，至少保留 CSV 的
            if (items.value.length === 0 && newItems.length > 0) {
                items.value = newItems;
            }

            csvSuccessMsg.value = '成功匯入 ' + result.data.length + ' 筆商品';
            if (result.error) {
                csvSuccessMsg.value += '（' + result.error + '）';
            }

            // 切回手動模式讓使用者編輯
            formMode.value = 'manual';
        } catch (err) {
            csvError.value = '檔案讀取失敗：' + err.message;
        } finally {
            csvUploading.value = false;
            // 重置 file input，允許重複上傳同一檔案
            event.target.value = '';
        }
    };
    reader.onerror = () => {
        csvError.value = '檔案讀取失敗';
        csvUploading.value = false;
    };
    reader.readAsText(file);
};

/**
 * 拖放狀態（控制上傳區的 .dragging 視覺回饋）
 */
const isDragging = ref(false);

/**
 * 處理拖放上傳
 * 從 dataTransfer 取得檔案，走和 handleCsvUpload 相同的 FileReader 路徑
 * @param {DragEvent} event - drop event
 */
const handleDrop = (event) => {
    isDragging.value = false;
    const file = event.dataTransfer && event.dataTransfer.files && event.dataTransfer.files[0];
    if (!file) return;
    if (!file.name.endsWith('.csv')) {
        csvError.value = '請上傳 .csv 格式的檔案';
        return;
    }

    csvError.value = '';
    csvSuccessMsg.value = '';
    csvUploading.value = true;

    const reader = new FileReader();
    reader.onload = (e) => {
        try {
            const text = e.target.result;
            const result = parseCSV(text);

            if (result.error && result.data.length === 0) {
                csvError.value = result.error;
                csvUploading.value = false;
                return;
            }

            const newItems = result.data.map(d => ({
                id: nextId++,
                name: d.name,
                price: d.price,
                quantity: d.quantity,
                description: d.description
            }));

            const filledItems = items.value.filter(item =>
                item.name.trim() !== '' || item.price.trim() !== ''
            );

            items.value = [...filledItems, ...newItems];

            if (items.value.length === 0 && newItems.length > 0) {
                items.value = newItems;
            }

            csvSuccessMsg.value = '成功匯入 ' + result.data.length + ' 筆商品';
            if (result.error) {
                csvSuccessMsg.value += '（' + result.error + '）';
            }

            formMode.value = 'manual';
        } catch (err) {
            csvError.value = '檔案讀取失敗：' + err.message;
        } finally {
            csvUploading.value = false;
        }
    };
    reader.onerror = () => {
        csvError.value = '檔案讀取失敗';
        csvUploading.value = false;
    };
    reader.readAsText(file);
};

/**
 * 清除 CSV 提示訊息
 */
const clearCsvMessages = () => {
    csvError.value = '';
    csvSuccessMsg.value = '';
};
```

**2. 擴充 return 物件：**

在 `// 表單（Phase 58）` return 區塊之後加入：
```javascript
// CSV 匯入（Phase 58 Plan 02）
formMode, csvError, csvSuccessMsg, csvUploading, isDragging,
setFormMode, parseCSV, handleCsvUpload, handleDrop, clearCsvMessages,
```

**3. 更新檔案頭部文件註解「功能」：**
```
 * - CSV 匯入（前端解析 + 模式切換）
```

**重要設計決策：**
- CSV 解析完全在前端用 FileReader + 字串分割，不送後端（符合 STATE.md 決策）
- 表頭支援中英文別名（名稱/name/商品名稱/品名 等），降低使用者門檻
- 數量缺失預設 '0' = 無限上架（符合業務語意）
- CSV 匯入策略：保留已填寫的手動資料 + 追加 CSV 資料（不清空已有內容）
- 匯入成功後自動切回 'manual' 模式，讓使用者在表單中繼續修改
- file input 重置 value=''，允許重複上傳同一檔案
  </action>
  <verify>
    <automated>cd /Users/fishtv/Development/wp-plugins/buygo-plus-one && grep "parseCSV" includes/views/composables/useBatchCreate.js && grep "handleCsvUpload" includes/views/composables/useBatchCreate.js && grep "handleDrop" includes/views/composables/useBatchCreate.js && grep "isDragging" includes/views/composables/useBatchCreate.js && grep "formMode" includes/views/composables/useBatchCreate.js && grep "csvError" includes/views/composables/useBatchCreate.js && grep "csvSuccessMsg" includes/views/composables/useBatchCreate.js && echo "PASS"</automated>
  </verify>
  <done>
    - formMode ref ('manual'/'csv') 管理模式切換，setFormMode() 切換時保留 items
    - parseCSV(text) 解析 CSV 文字，支援中英文表頭別名，驗證必填欄位
    - 數量缺失或非數字時預設 0（無限上架）
    - handleCsvUpload(event) 處理 file input 檔案上傳，FileReader 讀取 + 解析 + 填入 items
    - handleDrop(event) 處理拖放上傳，從 dataTransfer 取得檔案走相同 FileReader 路徑
    - isDragging ref 控制拖放時的視覺回饋（.dragging CSS class）
    - CSV 匯入策略：保留已填寫資料 + 追加 CSV 資料，匯入後自動切回 manual
    - csvError/csvSuccessMsg 提供錯誤和成功提示訊息
  </done>
</task>

<task type="auto">
  <name>Task 2: CSV 匯入 UI — pill 切換 + 上傳區 + 匯入結果提示</name>
  <files>admin/partials/batch-create.php</files>
  <action>
在 batch-create.php 的表單步驟區塊中新增 CSV 相關 UI。

**1. 在 `<style>` 標籤中追加 CSV 專用 CSS（在 Plan 01 CSS 之後）：**

```css
/* ===== Phase 58 Plan 02: CSV 匯入樣式 ===== */

/* 手機版 pill 切換 */
.bp-pill-group {
    display: inline-flex;
    border-radius: 8px;
    border: 1px solid #e2e8f0;
    overflow: hidden;
    background: #f8fafc;
}
.bp-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 16px;
    border: none;
    background: transparent;
    font-size: 13px;
    font-weight: 600;
    color: #64748b;
    cursor: pointer;
    transition: all 0.2s;
    white-space: nowrap;
}
.bp-pill.active {
    background: #2563EB;
    color: white;
}
.bp-pill:not(.active):hover {
    background: #e2e8f0;
}

/* CSV 上傳區域 */
.bp-csv-upload {
    border: 2px dashed #cbd5e1;
    border-radius: 12px;
    padding: 32px 16px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
    background: #f8fafc;
}
.bp-csv-upload:hover {
    border-color: #2563EB;
    background: #f0f5ff;
}
.bp-csv-upload.dragging {
    border-color: #2563EB;
    background: #eff6ff;
}

/* 提示訊息 */
.bp-toast {
    display: flex;
    align-items: flex-start;
    gap: 8px;
    padding: 12px 16px;
    border-radius: 8px;
    font-size: 13px;
    line-height: 1.5;
    margin-bottom: 12px;
}
.bp-toast-success {
    background: #f0fdf4;
    color: #166534;
    border: 1px solid #bbf7d0;
}
.bp-toast-error {
    background: #fef2f2;
    color: #991b1b;
    border: 1px solid #fecaca;
}

/* 桌面版 CSV 按鈕 */
.bp-csv-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 14px;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    background: white;
    font-size: 13px;
    font-weight: 600;
    color: #475569;
    cursor: pointer;
    transition: all 0.2s;
    white-space: nowrap;
}
.bp-csv-btn:hover {
    border-color: #2563EB;
    color: #2563EB;
    background: #f0f5ff;
}
```

**2. 手機版：在配額進度條和卡片表單之間插入 pill 切換和 CSV 上傳區**

在 `<!-- === 手機版配額進度條 (FORM-04) === -->` 區塊之後、`<!-- === 表單主體 === -->` 之前，插入：

```html
  <!-- === 手機版 pill 切換 (FORM-05) === -->
  <div class="bp-mobile-only">
    <div class="px-4 pt-3">
      <div class="bp-pill-group">
        <button @click="setFormMode('manual')" class="bp-pill" :class="{ active: formMode === 'manual' }">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
          </svg>
          手動輸入
        </button>
        <button @click="setFormMode('csv')" class="bp-pill" :class="{ active: formMode === 'csv' }">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"></path>
          </svg>
          CSV 匯入
        </button>
      </div>
    </div>
  </div>
```

**3. 在表單主體中加入 CSV 提示訊息和 CSV 上傳區**

在 `<!-- === 表單主體 === -->` 的 `<div class="flex-1 overflow-y-auto">` 開頭，在手機版卡片之前加入：

```html
    <!-- CSV 匯入結果提示（成功/錯誤） -->
    <div class="px-4 pt-3" v-if="csvSuccessMsg || csvError">
      <div v-if="csvSuccessMsg" class="bp-toast bp-toast-success">
        <svg class="w-4 h-4 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>{{ csvSuccessMsg }}</span>
        <button @click="clearCsvMessages" class="ml-auto text-green-500 hover:text-green-700">&times;</button>
      </div>
      <div v-if="csvError" class="bp-toast bp-toast-error">
        <svg class="w-4 h-4 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <span>{{ csvError }}</span>
        <button @click="clearCsvMessages" class="ml-auto text-red-500 hover:text-red-700">&times;</button>
      </div>
    </div>

    <!-- 手機版 CSV 上傳區（formMode === 'csv' 時顯示，支援點擊和拖放） -->
    <div v-if="formMode === 'csv'" class="bp-mobile-only px-4 py-4">
      <label class="bp-csv-upload block" :class="{ dragging: isDragging }"
        @dragover.prevent="isDragging = true"
        @dragleave.prevent="isDragging = false"
        @drop.prevent="handleDrop">
        <input type="file" accept=".csv" @change="handleCsvUpload" class="hidden">
        <div class="flex flex-col items-center gap-2">
          <svg class="w-10 h-10 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"></path>
          </svg>
          <p class="text-sm font-semibold text-slate-600">點擊或拖放 CSV 檔案到此處</p>
          <p class="text-xs text-slate-400">欄位：名稱、售價、數量、描述</p>
        </div>
      </label>
    </div>
```

**4. 桌面版：在配額 badge 旁邊加入「匯入 CSV」按鈕**

在 Plan 01 的桌面版配額 badge 區塊（`<div class="bp-desktop-only">` 內的 flex 容器），在配額 badge `<span>` 之後、`</div>` 之前加入：

```html
      <!-- 桌面版右側按鈕區 -->
      <div class="ml-auto flex items-center gap-2">
        <label class="bp-csv-btn">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"></path>
          </svg>
          匯入 CSV
          <input type="file" accept=".csv" @change="handleCsvUpload" class="hidden">
        </label>
      </div>
```

**5. 手機版卡片和桌面版表格的顯示條件：**

手機版卡片的 `<div class="bp-mobile-only px-4 py-4">` 加入條件：只在 manual 模式（或已有資料時）顯示。但設計需求是「切換到 CSV 模式時保留已填寫的手動資料」——所以手動表單在 CSV 模式下隱藏，但資料保留在 items 中。

修改手機版卡片區塊：
```html
<div v-if="formMode === 'manual'" class="bp-mobile-only px-4 py-4">
```

桌面版表格不需要改（桌面版沒有模式切換概念，「匯入 CSV」是獨立按鈕，匯入後直接顯示在表格中）。

**對照設計圖的關鍵 UI 細節：**
- 手機版（批量上架 - 手機版.png）：pill 按鈕在配額進度條下方，「手動輸入」有筆圖示，「CSV 匯入」有上傳圖示
- 桌面版（批量上架 - 手機版-1.png）：右上角「匯入 CSV」按鈕（上傳圖示 + 文字），在「批量上架 (N)」按鈕左邊（批量上架按鈕是 Phase 59）
- CSV 上傳區：虛線邊框 + 上傳圖示 + 提示文字
- 匯入成功：綠色提示條顯示「成功匯入 N 筆商品」
- 匯入失敗：紅色提示條顯示錯誤訊息
  </action>
  <verify>
    <automated>cd /Users/fishtv/Development/wp-plugins/buygo-plus-one && grep "bp-pill" admin/partials/batch-create.php && grep "bp-csv-upload" admin/partials/batch-create.php && grep "bp-csv-btn" admin/partials/batch-create.php && grep "handleCsvUpload" admin/partials/batch-create.php && grep "handleDrop" admin/partials/batch-create.php && grep "isDragging" admin/partials/batch-create.php && grep "dragover.prevent" admin/partials/batch-create.php && grep "csvSuccessMsg" admin/partials/batch-create.php && grep "csvError" admin/partials/batch-create.php && grep "formMode" admin/partials/batch-create.php && echo "PASS"</automated>
  </verify>
  <done>
    - 手機版有「手動輸入」/「CSV 匯入」pill 按鈕（bp-pill-group），可切換 formMode
    - 桌面版有「匯入 CSV」按鈕（bp-csv-btn）在配額 badge 旁邊
    - CSV 上傳區（bp-csv-upload）支援點擊 file input 和拖放上傳（@dragover.prevent + @drop.prevent="handleDrop"）
    - 拖放時 isDragging 控制 .dragging CSS class 提供視覺回饋（邊框變藍 + 背景變淺藍）
    - 匯入成功顯示綠色提示（bp-toast-success），匯入失敗顯示紅色提示（bp-toast-error）
    - 切換模式時保留 items 資料（formMode 只控制顯示，不清空 items）
    - 手機版 manual 模式顯示卡片，csv 模式顯示上傳區
    - 桌面版表格始終顯示，CSV 按鈕是獨立功能（匯入後直接填入表格）
  </done>
</task>

</tasks>

<verification>
1. 手機版（<768px）：配額進度條下方有「手動輸入」「CSV 匯入」兩個 pill 按鈕
2. 點「CSV 匯入」→ 卡片表單隱藏，顯示 CSV 上傳區（虛線邊框 + 上傳圖示 + 「點擊或拖放 CSV 檔案到此處」文字）
3. 點「手動輸入」→ 切回卡片表單，已填寫的資料仍在
4. 點擊上傳區選擇有效 CSV → 綠色提示「成功匯入 N 筆」+ 自動切回手動模式 + 資料填入表單
5. 拖放 CSV 檔案到上傳區 → 上傳區邊框變藍（.dragging class），放下後走相同匯入流程
6. 上傳缺少名稱欄位的 CSV → 紅色提示「CSV 缺少『名稱』欄位」
7. 上傳有部分無效行的 CSV → 有效行填入 + 提示跳過的行
8. CSV 中數量欄位缺失或空白 → 預設填入 0
9. 桌面版（>=768px）：右上角有「匯入 CSV」按鈕，點擊可選擇檔案上傳
10. 桌面版匯入 CSV 後 → 資料直接填入表格行，可繼續編輯
</verification>

<success_criteria>
- 手機版 pill 切換正常，CSV 模式顯示上傳區，手動模式顯示卡片表單
- 桌面版「匯入 CSV」按鈕可觸發檔案上傳
- CSV 上傳區支援 file input 點擊和拖放上傳兩種方式，拖放時有視覺回饋
- 模式切換時保留已填寫的手動資料
- CSV 前端解析正確（支援中英文表頭，數量缺失預設 0）
- 格式錯誤時有紅色提示，成功時有綠色提示含匯入筆數
- 匯入後可在表單中繼續修改
</success_criteria>

<output>
After completion, create `.planning/phases/58-batch-form-and-csv/58-02-SUMMARY.md`
</output>
