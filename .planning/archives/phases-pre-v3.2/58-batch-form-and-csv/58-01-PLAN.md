---
phase: 58-batch-form-and-csv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/views/composables/useBatchCreate.js
  - admin/partials/batch-create.php
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-04

must_haves:
  truths:
    - "手機版以卡片形式顯示每個商品（標題「商品 #N」），每張卡片有商品名稱/售價/數量/描述欄位和刪除按鈕"
    - "桌面版以表格形式顯示（# / 商品名稱 / 售價 / 數量 / 描述 / 操作），行內直接編輯"
    - "兩種佈局可透過 CSS 響應式斷點自動切換（手機 <768px 卡片，桌面 >=768px 表格）"
    - "底部有「+ 新增商品」按鈕，點擊新增一個空白商品；刪除按鈕可移除商品（至少保留 1 個）"
    - "頂部顯示配額進度（手機版「商品配額：N / 上限」+ 進度條，桌面版 badge「配額 N/上限」），新增或刪除商品時即時更新"
    - "點擊「開始填寫」按鈕後，畫面從數量選擇切換到商品表單填寫介面"
  artifacts:
    - path: "includes/views/composables/useBatchCreate.js"
      provides: "表單狀態管理（items 陣列 + 新增/刪除/配額進度）"
      exports: ["useBatchCreate"]
    - path: "admin/partials/batch-create.php"
      provides: "響應式表單 UI（手機卡片 + 桌面表格 + 配額進度條）"
      contains: "batch-create-page-template"
  key_links:
    - from: "admin/partials/batch-create.php"
      to: "includes/views/composables/useBatchCreate.js"
      via: "Vue template 綁定 items/addItem/removeItem/quotaProgress"
      pattern: "v-for.*items"
    - from: "includes/views/composables/useBatchCreate.js"
      to: "startFilling"
      via: "step.value = 'form' 啟動表單步驟"
      pattern: "step\\.value.*=.*form"
---

<objective>
實作批量上架的響應式表單 UI — 手機版卡片式 + 桌面版表格式，包含商品 CRUD 和配額進度顯示。

Purpose: 賣家從數量選擇進入後，在表單中填寫多個商品資料。手機版以卡片形式展示每個商品，桌面版以表格行內編輯。配額進度即時更新，新增/刪除商品自如。
Output: 響應式表單 UI + composable 表單狀態管理
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-route-and-quantity-selection/57-02-SUMMARY.md

<!-- 設計圖參考 -->
@.planning/designs/批量上架 - 手機版.png (手機版卡片式表單 — 商品 #1, #2 卡片 + 手動輸入/CSV 匯入 pill + 配額進度條)
@.planning/designs/批量上架 - 手機版-1.png (桌面版表格式表單 — # / 商品名稱 / 售價 / 數量 / 描述 / 操作 + 配額 badge + 匯入 CSV 按鈕)

<!-- 現有程式碼（Phase 57 產出） -->
@includes/views/composables/useBatchCreate.js
@admin/partials/batch-create.php
@admin/js/components/BatchCreatePage.js

<interfaces>
<!-- useBatchCreate composable 現有狀態（Phase 57 產出） -->
```javascript
function useBatchCreate() {
    // 步驟控制
    const step = ref('select');       // 'select' | 'form'

    // 數量選擇（Phase 57 已完成）
    const quantity = computed(...)     // 當前選擇的數量（0-20）
    const selectedPreset, customQuantity, presetOptions, selectPreset, onCustomInput

    // 配額（Phase 57 已完成）
    const quota = ref({ can_add, current, limit, message })
    const remaining = computed(...)    // 剩餘配額（Infinity = 無限制）
    const isOverQuota = computed(...)

    // 導航
    const goBack = ()                 // SPA 回到商品列表
    const startFilling = ()           // Phase 58：解除 step.value = 'form' 註解

    return { step, quantity, selectedPreset, customQuantity, presetOptions,
             selectPreset, onCustomInput, quota, quotaLoading, remaining,
             isOverQuota, canProceed, loadQuota, goBack, startFilling };
}
```

<!-- batch-create.php 模板結構（Phase 57） -->
```
<style>...</style>                    <!-- 批量上架專用 CSS -->
<script type="text/x-template">
  <div>
    <nav>返回 + 標題</nav>
    <div v-if="step === 'select'">    <!-- 數量選擇步驟 -->
    <div v-if="step === 'form'">      <!-- 表單步驟 — Phase 58 替換此空殼 -->
  </div>
</script>
<script>BatchCreatePage.js 載入</script>
```

<!-- 配額 API 回應格式 -->
GET /wp-json/buygo-plus-one/v1/products/limit-check
Response: { success: true, data: { can_add: true, current: 12, limit: 50, message: "..." } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useBatchCreate composable 擴充 — 表單狀態管理</name>
  <files>includes/views/composables/useBatchCreate.js</files>
  <action>
在現有 useBatchCreate.js 中擴充表單填寫邏輯。在 `// ========== 導航 ==========` 之前新增表單區塊。

**1. 解除 startFilling() 的註解：**

將 `startFilling()` 方法中的：
```javascript
// Phase 58 實作：step.value = 'form';
console.log('開始填寫', quantity.value, '個商品');
```
改為：
```javascript
step.value = 'form';
initItems(quantity.value);
```

**2. 新增表單狀態管理區塊（在導航區塊前）：**

```javascript
// ========== 表單狀態（Phase 58） ==========

/**
 * 商品表單項目陣列
 * 每個 item: { id: number, name: '', price: '', quantity: '0', description: '' }
 * id 用於 Vue v-for :key 追蹤
 */
const items = ref([]);
let nextId = 1;

/**
 * 建立單一空白商品物件
 */
const createEmptyItem = () => ({
    id: nextId++,
    name: '',
    price: '',
    quantity: '0',
    description: ''
});

/**
 * 初始化 N 個空白商品
 * 由 startFilling() 呼叫
 */
const initItems = (count) => {
    items.value = [];
    nextId = 1;
    for (let i = 0; i < count; i++) {
        items.value.push(createEmptyItem());
    }
};

/**
 * 新增一個空白商品
 */
const addItem = () => {
    items.value.push(createEmptyItem());
};

/**
 * 刪除指定商品（至少保留 1 個）
 */
const removeItem = (id) => {
    if (items.value.length <= 1) return;
    items.value = items.value.filter(item => item.id !== id);
};

/**
 * 目前表單中的商品數量
 */
const itemCount = computed(() => items.value.length);

/**
 * 配額進度（已用 + 目前表單中的商品數量）
 * 用於頂部進度條顯示
 */
const quotaUsed = computed(() => quota.value.current + itemCount.value);

/**
 * 配額進度百分比（0-100）
 * limit === 0 表示無限制，回傳 0（不顯示進度條）
 */
const quotaPercent = computed(() => {
    if (quota.value.limit === 0) return 0;
    return Math.min(100, Math.round((quotaUsed.value / quota.value.limit) * 100));
});

/**
 * 表單階段的「超額」判斷
 * 和數量選擇階段不同 — 這裡用 itemCount 而非 quantity
 */
const isFormOverQuota = computed(() => {
    if (quota.value.limit === 0) return false;
    return quotaUsed.value > quota.value.limit;
});
```

**3. 擴充 return 物件：**

在現有 return 的 `// 導航` 之前加入：
```javascript
// 表單（Phase 58）
items, createEmptyItem, initItems, addItem, removeItem,
itemCount, quotaUsed, quotaPercent, isFormOverQuota,
```

**4. 更新檔案頭部文件註解：**

在「功能」列表加入：
```
 * - 表單狀態管理（items CRUD + 配額進度）
```

在「步驟控制」加入：
```
 * - 'form'   = 表單填寫（Phase 58 實作 ✓）
```

**重要設計決策：**
- `price` 和 `quantity` 存為字串（和 customQuantity 一致），避免 number input 的 0 預設值問題
- `quantity` 預設 '0' 表示無限上架（符合業務需求：0 = 無限）
- `id` 是前端追蹤用的遞增數字，非資料庫 ID
- `quotaUsed` = 已用配額(current) + 表單商品數(itemCount)，動態反映新增/刪除
- `isFormOverQuota` 獨立計算（不複用 isOverQuota，因為後者依賴 quantity 是數量選擇值）
  </action>
  <verify>
    <automated>cd /Users/fishtv/Development/wp-plugins/buygo-plus-one && grep "initItems" includes/views/composables/useBatchCreate.js && grep "addItem" includes/views/composables/useBatchCreate.js && grep "removeItem" includes/views/composables/useBatchCreate.js && grep "quotaPercent" includes/views/composables/useBatchCreate.js && grep "step.value = 'form'" includes/views/composables/useBatchCreate.js && echo "PASS"</automated>
  </verify>
  <done>
    - startFilling() 已解除註解，呼叫 step.value = 'form' + initItems(quantity)
    - items ref 陣列管理表單商品（每項有 id/name/price/quantity/description）
    - addItem() 新增空白商品，removeItem(id) 刪除指定商品（至少保留 1 個）
    - quotaUsed/quotaPercent/isFormOverQuota 即時反映表單商品數和配額狀態
    - 所有新狀態已在 return 中匯出
  </done>
</task>

<task type="auto">
  <name>Task 2: 響應式表單 UI — 手機卡片 + 桌面表格 + 配額進度</name>
  <files>admin/partials/batch-create.php</files>
  <action>
替換 batch-create.php 中 `step === 'form'` 的空殼區塊（行 149-155），實作完整響應式表單 UI。同時在 `<style>` 區塊新增表單專用 CSS。

**1. 在現有 `<style>` 標籤中追加表單 CSS（在 `.batch-preset-btn .bp-unit` 之後）：**

```css
/* ===== Phase 58: 批量表單樣式 ===== */

/* 配額進度條 */
.bp-quota-bar {
    height: 6px;
    border-radius: 3px;
    background: #e2e8f0;
    overflow: hidden;
    flex: 1;
    min-width: 80px;
}
.bp-quota-bar-fill {
    height: 100%;
    border-radius: 3px;
    background: #2563EB;
    transition: width 0.3s ease;
}
.bp-quota-bar-fill.over {
    background: #dc2626;
}

/* 手機版卡片 */
.bp-card {
    background: white;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 16px;
    margin-bottom: 12px;
}
.bp-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
}
.bp-card-title {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 700;
    font-size: 15px;
    color: #1e293b;
}
.bp-card-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: #2563EB;
    color: white;
    font-size: 12px;
    font-weight: 700;
}
.bp-field-label {
    font-size: 13px;
    font-weight: 600;
    color: #475569;
    margin-bottom: 4px;
}
.bp-field-label .required {
    color: #dc2626;
}
.bp-input {
    width: 100%;
    padding: 8px 12px;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    font-size: 14px;
    outline: none;
    transition: border-color 0.2s;
}
.bp-input:focus {
    border-color: #2563EB;
    box-shadow: 0 0 0 2px rgba(37,99,235,0.1);
}
.bp-input::placeholder {
    color: #94a3b8;
}
.bp-delete-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 8px;
    border: none;
    background: transparent;
    color: #94a3b8;
    cursor: pointer;
    transition: all 0.2s;
}
.bp-delete-btn:hover {
    background: #fef2f2;
    color: #dc2626;
}
.bp-add-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    width: 100%;
    padding: 12px;
    border: 2px dashed #cbd5e1;
    border-radius: 12px;
    background: transparent;
    color: #2563EB;
    font-weight: 600;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.2s;
}
.bp-add-btn:hover {
    border-color: #2563EB;
    background: #f0f5ff;
}

/* 桌面版表格 */
.bp-table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
}
.bp-table th {
    font-size: 12px;
    font-weight: 600;
    color: #64748b;
    text-align: left;
    padding: 8px 12px;
    border-bottom: 1px solid #e2e8f0;
    white-space: nowrap;
}
.bp-table td {
    padding: 8px 12px;
    vertical-align: middle;
    border-bottom: 1px solid #f1f5f9;
}
.bp-table .bp-row-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: #eff6ff;
    color: #2563EB;
    font-size: 12px;
    font-weight: 700;
}
.bp-table input {
    width: 100%;
    padding: 6px 10px;
    border: 1px solid #e2e8f0;
    border-radius: 6px;
    font-size: 14px;
    outline: none;
    transition: border-color 0.2s;
}
.bp-table input:focus {
    border-color: #2563EB;
    box-shadow: 0 0 0 2px rgba(37,99,235,0.1);
}
.bp-table input::placeholder {
    color: #94a3b8;
}

/* 響應式斷點：768px */
.bp-mobile-only { display: block; }
.bp-desktop-only { display: none; }
@media (min-width: 768px) {
    .bp-mobile-only { display: none; }
    .bp-desktop-only { display: block; }
}
```

**2. 替換 `v-if="step === 'form'"` 空殼為完整表單 UI：**

將行 149-155（Phase 58 placeholder）替換為以下結構：

```html
<!-- 步驟 2: 表單填寫 (FORM-01 ~ FORM-05) -->
<div v-if="step === 'form'" class="flex-1 flex flex-col">

  <!-- === 桌面版頂部導航區 (FORM-04 桌面版) === -->
  <div class="bp-desktop-only">
    <div class="flex items-center gap-3 px-6 py-3 border-b border-slate-200 bg-white">
      <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-semibold"
        :class="isFormOverQuota ? 'bg-red-50 text-red-600' : 'bg-blue-50 text-blue-600'">
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
        </svg>
        配額 {{ quotaUsed }}/{{ quota.limit === 0 ? '∞' : quota.limit }}
      </span>
    </div>
  </div>

  <!-- === 手機版配額進度條 (FORM-04) === -->
  <div class="bp-mobile-only">
    <div class="px-4 py-3 bg-white border-b border-slate-200">
      <div class="flex items-center gap-3">
        <svg class="w-4 h-4 shrink-0" :class="isFormOverQuota ? 'text-red-500' : 'text-blue-500'" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
        </svg>
        <span class="text-sm font-semibold" :class="isFormOverQuota ? 'text-red-600' : 'text-slate-700'">
          商品配額：{{ quotaUsed }} / {{ quota.limit === 0 ? '∞' : quota.limit }}
        </span>
        <div v-if="quota.limit > 0" class="bp-quota-bar">
          <div class="bp-quota-bar-fill" :class="{ over: isFormOverQuota }" :style="{ width: quotaPercent + '%' }"></div>
        </div>
      </div>
    </div>
  </div>

  <!-- === 表單主體 === -->
  <div class="flex-1 overflow-y-auto">

    <!-- ===== 手機版：卡片式表單 (FORM-01) ===== -->
    <div class="bp-mobile-only px-4 py-4">
      <div v-for="(item, index) in items" :key="item.id" class="bp-card">
        <!-- 卡片標題 -->
        <div class="bp-card-header">
          <div class="bp-card-title">
            <span class="bp-card-num">{{ index + 1 }}</span>
            商品 #{{ index + 1 }}
          </div>
          <button v-if="items.length > 1" @click="removeItem(item.id)" class="bp-delete-btn" title="刪除此商品">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
            </svg>
          </button>
        </div>
        <!-- 商品名稱 -->
        <div class="mb-3">
          <div class="bp-field-label">商品名稱 <span class="required">*</span></div>
          <input type="text" v-model="item.name" class="bp-input" placeholder="輸入商品名稱">
        </div>
        <!-- 售價 + 數量（並排） -->
        <div class="flex gap-3 mb-3">
          <div class="flex-1">
            <div class="bp-field-label">售價 <span class="required">*</span></div>
            <input type="number" v-model="item.price" class="bp-input" placeholder="售價" min="0">
          </div>
          <div class="flex-1">
            <div class="bp-field-label">數量 (0=無限)</div>
            <input type="number" v-model="item.quantity" class="bp-input" placeholder="數量" min="0">
          </div>
        </div>
        <!-- 描述 -->
        <div>
          <div class="bp-field-label">描述（選填）</div>
          <input type="text" v-model="item.description" class="bp-input" placeholder="商品描述...">
        </div>
      </div>

      <!-- 新增商品按鈕 (FORM-03) -->
      <button @click="addItem" class="bp-add-btn">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
        </svg>
        新增商品
      </button>
    </div>

    <!-- ===== 桌面版：表格式表單 (FORM-02) ===== -->
    <div class="bp-desktop-only px-6 py-4">
      <div class="bg-white rounded-xl border border-slate-200 overflow-hidden">
        <table class="bp-table">
          <thead>
            <tr>
              <th style="width:48px">#</th>
              <th>商品名稱</th>
              <th style="width:120px">售價</th>
              <th style="width:100px">數量</th>
              <th>描述</th>
              <th style="width:60px">操作</th>
            </tr>
          </thead>
          <tbody>
            <tr v-for="(item, index) in items" :key="item.id">
              <td><span class="bp-row-num">{{ index + 1 }}</span></td>
              <td><input type="text" v-model="item.name" placeholder="商品名稱"></td>
              <td><input type="number" v-model="item.price" placeholder="售價" min="0"></td>
              <td><input type="number" v-model="item.quantity" placeholder="數量" min="0"></td>
              <td><input type="text" v-model="item.description" placeholder="描述（選填）"></td>
              <td>
                <button v-if="items.length > 1" @click="removeItem(item.id)" class="bp-delete-btn" title="刪除">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                  </svg>
                </button>
              </td>
            </tr>
          </tbody>
        </table>
        <!-- 新增商品列按鈕 (FORM-03) -->
        <div class="p-3 border-t border-slate-100">
          <button @click="addItem" class="bp-add-btn">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            新增商品列
          </button>
        </div>
      </div>
    </div>

  </div><!-- /overflow-y-auto -->

</div><!-- /step === 'form' -->
```

**3. 更新頂部導航列（行 44-51）以在表單步驟顯示配額 badge（桌面版）：**

在現有標題 `<h1>` 之後，加入桌面版配額 badge — 但注意只有在 `step === 'form'` 且是桌面版時才額外顯示在頂部 bar。由於桌面版的配額 badge 已在 form 區塊內的 `bp-desktop-only` 區域處理，頂部導航列不需要改動。

**對照設計圖的關鍵 UI 細節：**
- 手機版（批量上架 - 手機版.png）：配額進度條在 pill 按鈕上方，卡片有藍色數字圓圈 + "商品 #N"，售價和數量並排，描述在下方
- 桌面版（批量上架 - 手機版-1.png）：表頭 # / 商品名稱 / 售價 / 數量 / 描述 / 操作，右上角有「配額 12/50」badge + 「匯入 CSV」按鈕 + 「批量上架 (2)」按鈕
- 「+ 新增商品」/「+ 新增商品列」：手機版文字是「新增商品」，桌面版是「新增商品列」
- 刪除按鈕只在商品數 > 1 時顯示（保護最少 1 個）

**注意：**
- 手機版「手動輸入/CSV 匯入」pill 按鈕在 Plan 02 實作（FORM-05）
- 桌面版「匯入 CSV」按鈕在 Plan 02 實作（FORM-05）
- 桌面版「批量上架 (N)」按鈕在 Phase 59 實作（SUBMIT-01）
- 底部提示文字在 Phase 59 實作（SUBMIT-04）
  </action>
  <verify>
    <automated>cd /Users/fishtv/Development/wp-plugins/buygo-plus-one && grep "bp-card" admin/partials/batch-create.php && grep "bp-table" admin/partials/batch-create.php && grep "bp-quota-bar" admin/partials/batch-create.php && grep "bp-mobile-only" admin/partials/batch-create.php && grep "bp-desktop-only" admin/partials/batch-create.php && grep "addItem" admin/partials/batch-create.php && grep "removeItem" admin/partials/batch-create.php && echo "PASS"</automated>
  </verify>
  <done>
    - 手機版卡片式表單：每個商品一張 bp-card，標題「商品 #N」+藍色數字圓圈，含 4 個欄位 + 刪除按鈕
    - 桌面版表格式表單：bp-table 行內直接編輯，表頭 # / 商品名稱 / 售價 / 數量 / 描述 / 操作
    - 兩種佈局透過 bp-mobile-only / bp-desktop-only + @media (min-width: 768px) 響應式切換
    - 底部「+ 新增商品」/「+ 新增商品列」按鈕可新增空白商品
    - 刪除按鈕在 items.length > 1 時顯示（保護最少 1 個）
    - 手機版配額進度條（文字 + 進度條）和桌面版配額 badge 即時更新
  </done>
</task>

</tasks>

<verification>
1. 在數量選擇頁選 5 → 點「開始填寫」→ 頁面切換到表單步驟，顯示 5 個空白商品
2. 手機版（<768px）：每個商品顯示為卡片，有「商品 #1」~「商品 #5」標題
3. 桌面版（>=768px）：商品以表格形式顯示，表頭 # / 商品名稱 / 售價 / 數量 / 描述 / 操作
4. 輸入商品名稱、售價等欄位 → v-model 雙向綁定正常
5. 點「+ 新增商品」→ 底部新增一個空白商品，配額數字更新
6. 點垃圾桶刪除商品 → 商品消失，配額數字更新；只剩 1 個時刪除按鈕隱藏
7. 配額進度條百分比隨商品數量即時變化
</verification>

<success_criteria>
- 手機版卡片式 + 桌面版表格式雙佈局完整運作
- 商品 CRUD（新增/刪除/編輯）正常，最少保留 1 個商品
- 配額進度即時更新（手機版進度條 + 桌面版 badge）
- step 從 'select' 正確切換到 'form'
</success_criteria>

<output>
After completion, create `.planning/phases/58-batch-form-and-csv/58-01-SUMMARY.md`
</output>
