---
phase: 11-按鈕與狀態標籤遷移
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/components/button.css
  - design-system/components/status-tag.css
autonomous: true

must_haves:
  truths:
    - "設計系統包含 .btn-danger class（紅色背景白色文字）"
    - "設計系統包含 .status-tag-danger class（作為 .status-tag-error 的別名）"
  artifacts:
    - path: "design-system/components/button.css"
      provides: ".btn-danger class"
      contains: ".btn-danger"
    - path: "design-system/components/status-tag.css"
      provides: ".status-tag-danger alias"
      contains: ".status-tag-danger"
  key_links:
    - from: "design-system/components/button.css"
      to: "design-system/tokens/colors.css"
      via: "CSS variable"
      pattern: "var\\(--color-error"
---

<objective>
補充設計系統缺失的 danger classes

Purpose: 確保設計系統包含所有頁面遷移所需的 class
Output: 更新後的 button.css 和 status-tag.css
</objective>

<execution_context>
@/Users/fishtv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fishtv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@design-system/components/button.css
@design-system/components/status-tag.css
@design-system/tokens/colors.css
@.planning/phases/11-按鈕與狀態標籤遷移/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 .btn-danger class</name>
  <files>design-system/components/button.css</files>
  <action>
在 `.btn-secondary` 之後添加 `.btn-danger` class：

```css
/* Danger Button */
.btn-danger {
  background-color: var(--color-error);
  color: white;
}

.btn-danger:hover:not(:disabled) {
  background-color: var(--color-error-600);
}
```

**注意**：這是危險操作按鈕（刪除、取消等）專用樣式。
  </action>
  <verify>
```bash
grep -n "btn-danger" /Users/fishtv/Development/buygo-plus-one-dev/design-system/components/button.css
```
  </verify>
  <done>.btn-danger class 已添加到 button.css</done>
</task>

<task type="auto">
  <name>Task 2: 添加 .status-tag-danger alias</name>
  <files>design-system/components/status-tag.css</files>
  <action>
在 `.status-tag-error` 之後添加 `.status-tag-danger` 作為別名（保留現有的 .status-tag-error）：

```css
/* Danger Status (alias for .status-tag-error, for consistency with .btn-danger naming) */
.status-tag-danger {
  background-color: var(--color-error-50);
  color: var(--color-error-600);
}
```

**重要**：
- 這是別名，不是替代 .status-tag-error
- .status-tag-error 保留供既有代碼使用
- .status-tag-danger 供新代碼使用，與 .btn-danger 命名一致
  </action>
  <verify>
```bash
# 確認兩個 class 都存在
grep -n "status-tag-error\|status-tag-danger" /Users/fishtv/Development/buygo-plus-one-dev/design-system/components/status-tag.css
```
  </verify>
  <done>.status-tag-danger alias 已添加（.status-tag-error 保留）</done>
</task>

</tasks>

<verification>
1. 設計系統 CSS 檔案語法正確
2. .btn-danger 和 .status-tag-danger 都已添加
3. 原有的 .status-tag-error 未被移除

```bash
# 驗證新 classes
grep "btn-danger" /Users/fishtv/Development/buygo-plus-one-dev/design-system/components/button.css
grep "status-tag-danger" /Users/fishtv/Development/buygo-plus-one-dev/design-system/components/status-tag.css
# 驗證原有 class 保留
grep "status-tag-error" /Users/fishtv/Development/buygo-plus-one-dev/design-system/components/status-tag.css
```
</verification>

<success_criteria>
- [ ] .btn-danger class 已添加到 button.css
- [ ] .status-tag-danger class 已添加到 status-tag.css
- [ ] .status-tag-error class 保留（未被替換）
</success_criteria>

<output>
After completion, create `.planning/phases/11-按鈕與狀態標籤遷移/11-01-SUMMARY.md`
</output>
