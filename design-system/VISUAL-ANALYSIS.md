# BuyGo+1 視覺風格分析報告

> **目的**: 提取訂單頁面(最舒服的狀態)的設計規範,並分析其他頁面的不一致之處
> **日期**: 2026-01-27
> **狀態**: 僅分析,未修改任何程式碼

---

## 📊 訂單頁面 (基準 - 最舒服的狀態)

### 顏色系統

| 用途 | 顏色值 | CSS 類別 | 備註 |
|------|--------|----------|------|
| **背景** | `#F8FAFC` (slate-50) | `bg-slate-50` | 頁面主背景 |
| **卡片背景** | `#FFFFFF` (white) | `bg-white` | 表格、卡片背景 |
| **主要文字** | `#0F172A` (slate-900) | `text-slate-900` | 標題、重要文字 |
| **次要文字** | `#64748B` (slate-500) | `text-slate-500` | 輔助資訊 |
| **淺文字** | `#CBD5E1` (slate-300) | `text-slate-300` | 麵包屑分隔符 |
| **邊框** | `#E2E8F0` (slate-200) | `border-slate-200` | 卡片、表格邊框 |
| **Primary** | `#2563EB` (blue-600) | `bg-primary`, `text-primary` | 主要按鈕、連結 |
| **懸停背景** | `#F8FAFC` (slate-50) | `hover:bg-slate-50` | 表格行懸停 |
| **輸入框背景** | `#F1F5F9` (slate-100) | `bg-slate-100` | 搜尋框背景 |

**成功案例** (語意色):
- 橘色按鈕: `bg-orange-50`, `text-orange-600`, `border-orange-200`
- 紅色按鈕: `bg-red-50`, `text-red-600`, `border-red-200`
- 藍色標籤: `bg-blue-50`, `text-blue-600`, `border-blue-200`

### 字體系統

| 類型 | 字體 | 大小 | 粗細 | 使用場景 |
|------|------|------|------|----------|
| **標題 H1** | Inter | `text-xl` (1.25rem / 20px) | `font-bold` (700) | 頁面標題 |
| **麵包屑** | Inter | `text-xs` (0.75rem / 12px) | `font-normal` (400) | 導航路徑 |
| **表格標題** | Inter | `text-xs` (0.75rem / 12px) | `font-semibold` (600) + `uppercase` | Table Header |
| **按鈕** | Inter | `text-xs` (0.75rem / 12px) | `font-medium` (500) | 所有按鈕 |
| **內文** | Inter | `text-sm` (0.875rem / 14px) | `font-normal` (400) | 一般內容 |

**字體家族**:
- Sans: `Inter, sans-serif`
- Mono: `Fira Code, monospace` (未在訂單頁使用)

### 間距系統

| 元素 | 間距 | 值 |
|------|------|-----|
| **Header 高度** | `h-16` | 64px (固定) |
| **Header 水平 Padding** | `px-4 md:px-6` | 16px → 24px (響應式) |
| **內容區 Padding** | `p-2 xs:p-4 md:p-6` | 8px → 16px → 24px |
| **按鈕 Padding** | `px-3 py-1.5` | 12px × 6px |
| **表格格子 Padding** | `px-4 py-3` | 16px × 12px |
| **元素間距 (小)** | `gap-2` | 8px |
| **元素間距 (中)** | `gap-3 md:gap-4` | 12px → 16px |
| **區塊間距** | `space-y-4 md:space-y-6` | 16px → 24px |

### 圓角系統

| 元素 | 圓角值 | CSS 類別 |
|------|--------|----------|
| **按鈕** | `0.5rem` (8px) | `rounded-lg` |
| **輸入框** | `0.5rem` (8px) | `rounded-lg` |
| **表格容器** | `1rem` (16px) | `rounded-2xl` |
| **標籤/徽章** | `9999px` (完全圓形) | `rounded-full` |
| **通知按鈕** | `9999px` (圓形) | `rounded-full` |

### 陰影系統

| 元素 | 陰影值 | CSS 類別 |
|------|--------|----------|
| **表格容器** | 微陰影 | `shadow-sm` |
| **按鈕** | 微陰影 | `shadow-sm` |

### 過渡動畫

| 元素 | 持續時間 | 緩動函數 | CSS 類別 |
|------|----------|----------|----------|
| **按鈕懸停** | 預設 | 預設 | `transition` |
| **搜尋框寬度** | 300ms | 預設 | `transition-all duration-300` |
| **搜尋框展開動畫** | 200ms | ease | `.search-slide` transition |
| **表格行懸停** | 預設 | 預設 | `transition` |

### 佈局模式

```
┌─────────────────────────────────────────────┐
│ Header (固定高度 64px, sticky on mobile)    │
│ bg-white, border-b border-slate-200        │
├─────────────────────────────────────────────┤
│                                             │
│  Content Area (flex-1, overflow-auto)       │
│  bg-slate-50/50                            │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ Container (max-w-7xl mx-auto)         │ │
│  │ p-2 xs:p-4 md:p-6                     │ │
│  │                                        │ │
│  │ ┌──────────────────────────────────┐ │ │
│  │ │ Table Card (rounded-2xl)         │ │ │
│  │ │ bg-white, shadow-sm, border      │ │ │
│  │ └──────────────────────────────────┘ │ │
│  └───────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘
```

---

## ❌ 商品頁面 (問題分析)

### 與訂單頁的差異

| 項目 | 訂單頁 | 商品頁 | 問題 |
|------|--------|--------|------|
| **Tailwind Config** | ❌ 無 | ✅ 有 (自訂顏色) | 不一致!商品頁有額外的 config |
| **Primary 顏色** | - | `#2563EB` | 商品頁在 config 中定義,訂單頁直接用 Tailwind |
| **背景顏色** | - | `#F8FAFC` | 商品頁多了 `background` token |
| **CTA 顏色** | - | `#F97316` (orange-500) | 商品頁定義但未統一使用 |
| **自訂螢幕斷點** | - | `xs: 375px` | 只有商品頁有 |

### 顏色不一致問題

**商品頁的 Tailwind Config** (在 PHP 檔案開頭):
```javascript
tailwind.config = {
    theme: {
        extend: {
            colors: {
                primary: '#2563EB',    // 與 Tailwind blue-600 相同
                secondary: '#3B82F6',  // blue-500
                cta: '#F97316',        // orange-500
                surface: '#FFFFFF',    // white
                background: '#F8FAFC', // slate-50
            }
        }
    }
}
```

**問題**:
1. ❌ 這些顏色定義只在**商品頁**有效
2. ❌ 訂單頁沒有這個 config,所以不能用 `bg-primary`
3. ❌ 每個頁面都要重複定義,造成維護困難

---

## ❌ 客戶頁面 (問題分析)

### CSS 檔案對比

**訂單頁 CSS** (`admin/css/orders.css`):
- 自訂 scrollbar 樣式 ✅
- 搜尋動畫 transition ✅
- Vue cloak ✅

**客戶頁 CSS** (`admin/css/customers.css`):
- 只有搜尋動畫 ⚠️
- **缺少** scrollbar 樣式 ❌
- 樣式最少

### 問題總結

| 頁面 | Scrollbar | 動畫 | 其他樣式 |
|------|-----------|------|----------|
| 訂單 | ✅ | ✅ | ✅ |
| 商品 | ✅ | ✅ | ✅ (最多) |
| 客戶 | ❌ | ✅ | ❌ (最少) |

---

## ❌ 出貨頁面 (問題分析)

### 待確認

需要檢查:
- [ ] shipment-details.php
- [ ] shipment-products.php

---

## 🎯 核心問題總結

### 1. Tailwind Config 分散問題

**現狀**:
- ❌ 商品頁有自己的 config (定義 primary, secondary, cta...)
- ❌ 訂單頁沒有 config
- ❌ 客戶頁沒有 config

**結果**:
- 無法統一使用 `bg-primary`, `text-cta` 等語意化 class
- 每個頁面的顏色定義可能不同

### 2. CSS 樣式重複問題

**Scrollbar 樣式重複** (products.css 和 orders.css 都有):
```css
::-webkit-scrollbar { width: 8px; height: 8px; }
::-webkit-scrollbar-track { background: #f1f5f9; }
::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
```

**動畫樣式重複** (3 個檔案都有):
```css
.search-slide-enter-active,
.search-slide-leave-active { transition: all 0.2s ease; }
```

### 3. 表格樣式不統一

**需要確認**:
- 每個頁面的表格 header 樣式是否一致?
- 表格行的 hover 效果是否一致?
- 表格邊框、間距是否一致?

---

## 💡 解決方案建議

### 方案 A: 建立統一的設計系統 CSS (推薦)

建立以下檔案結構:

```
design-system/
├── tokens/
│   ├── colors.css       # 統一的顏色變數
│   ├── typography.css   # 統一的字體
│   ├── spacing.css      # 統一的間距
│   └── effects.css      # scrollbar, shadows, animations
└── components/
    ├── tables.css       # 統一的表格樣式
    ├── buttons.css      # 統一的按鈕樣式
    └── forms.css        # 統一的表單樣式
```

**所有頁面都載入**:
```php
<link rel="stylesheet" href=".../design-system/tokens/colors.css" />
<link rel="stylesheet" href=".../design-system/tokens/effects.css" />
<link rel="stylesheet" href=".../design-system/components/tables.css" />
```

### 方案 B: 統一 Tailwind Config

在外掛的主 JS 檔案中定義一次 Tailwind config,所有頁面共用。

---

## 📋 下一步行動 (等待你確認)

### 階段 1: 視覺確認 (目前階段)

我會建立:
1. ✅ **Pencil 設計檔** - 展示統一後的視覺效果
2. ✅ **HTML Artifacts** - 可在瀏覽器預覽的設計方案
3. ✅ **並排對比圖** - 現況 vs 統一後

**你需要確認**:
- 顏色看起來舒服嗎?
- 間距看起來舒服嗎?
- 表格樣式看起來舒服嗎?

### 階段 2: 設計系統產生 (確認後)

我會建立:
- `design-system/tokens/` 所有設計 token
- `design-system/components/` 所有元件樣式
- `design-system/MASTER.md` 完整設計規範

### 階段 3: 程式碼遷移 (最後階段)

**只在你確認設計後才會執行**:
- 更新每個頁面載入設計系統 CSS
- 移除重複的 CSS 樣式
- 統一 Tailwind config

---

**你現在想要我做什麼?**

1. ✅ 繼續分析其他頁面 (shipment-details, shipment-products)?
2. ✅ 使用 Pencil 建立統一設計方案給你看?
3. ✅ 產生 HTML artifacts 讓你在瀏覽器預覽?
4. ✅ 其他?

---

**最後更新**: 2026-01-27
**狀態**: 僅分析,未修改程式碼
