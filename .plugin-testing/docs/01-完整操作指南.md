# 📖 完整操作指南 - AI 與你的協作工作流

## 目錄

1. [項目結構](#1-項目結構)
2. [三個工作流程](#2-三個工作流程)
3. [AI 的工作邏輯](#3-ai-的工作邏輯)
4. [詳細步驟說明](#4-詳細步驟說明)
5. [備份和回滾](#5-備份和回滾)
6. [快速參考](#6-快速參考)

---

## 1. 項目結構

### 根目錄概覽

```
/Users/fishtv/Development/
│
├─ .plugin-testing/              ← 統一框架 (AI 的大腦)
│  ├─ .setup/                    ← 初始化腳本
│  ├─ scripts/                   ← 工作腳本
│  ├─ templates/                 ← 項目模板
│  ├─ .ai-config/                ← AI 配置
│  └─ docs/                      ← 你在這裡
│
├─ buygo-plus-one/               ← 外掛 1 (只放代碼)
│  ├─ includes/
│  ├─ tests/
│  ├─ composer.json
│  └─ .phpunit.config
│
├─ test-plugins/                 ← 外掛 2 (新建立的)
│  └─ ...
│
├─ .git/                         ← 版本控制
├─ .gitignore                    ← 忽略規則
└─ README.md                     ← 根目錄說明
```

### 每個外掛的結構

```
plugin-name/
├─ includes/              ← 實際源代碼
├─ tests/
│  ├─ Unit/Services/
│  │  └─ ProductServiceBasicTest.php
│  ├─ bootstrap-unit.php (從 .plugin-testing 複製)
│  └─ bootstrap.php
├─ composer.json          ← 簡化版 (指向上層)
├─ phpunit-unit.xml       ← 簡化版 (指向上層)
├─ .phpunit.config        ← 項目特定配置
└─ TESTING.md             ← 項目文檔
```

---

## 2. 三個工作流程

### 工作流程 A: 測試現有代碼

```
【使用者】: "幫我測試 buygo-plus-one"

【AI 步驟】:
┌─────────────────────────────────────────┐
│ Step 1: 環境檢查                        │
│  cd /Users/fishtv/Development          │
│  檢查 .plugin-testing/ 是否完整         │
│  檢查 buygo-plus-one/ 是否存在          │
│                                         │
│ Step 2: 進入項目                        │
│  cd buygo-plus-one                     │
│                                         │
│ Step 3: 執行測試                        │
│  composer test                          │
│  (執行 phpunit -c phpunit-unit.xml)    │
│                                         │
│ Step 4: 分析結果                        │
│  ✅ 如果通過: "所有測試通過(7/7)"       │
│  ❌ 如果失敗: "X 個測試失敗，原因..."   │
│                                         │
│ Step 5: 報告                            │
│  顯示測試結果                           │
│  提出建議                               │
└─────────────────────────────────────────┘

【結果】: 你知道代碼的狀態
```

### 工作流程 B: 修改代碼並驗證

```
【使用者】: "我改了 ProductService，幫我驗證"

【AI 步驟】:
┌─────────────────────────────────────────┐
│ Step 1: 檢查修改                        │
│  進入 buygo-plus-one                   │
│  git status (查看哪些文件改變)         │
│  git diff (查看具體改變)                │
│                                         │
│ Step 2: 讀取修改內容                    │
│  讀取 includes/services/               │
│      class-product-service.php         │
│  理解改變了什麼                         │
│                                         │
│ Step 3: 執行測試                        │
│  composer test                          │
│                                         │
│ Step 4: 分析結果                        │
│  ✅ 如果通過: "修改是安全的"             │
│  ❌ 如果失敗: "檢測到問題..."           │
│      提出修復建議                       │
│                                         │
│ Step 5: 詢問備份                        │
│  "要建立 git commit 備份嗎?"            │
│                                         │
│ Step 6: 建立備份                        │
│  git add includes/services/             │
│         tests/Unit/Services/            │
│  git commit -m "改善計價邏輯..."        │
│  git log --oneline -1 (顯示備份點)      │
│                                         │
│ Step 7: 完成                            │
│  "修改已驗證並備份"                    │
└─────────────────────────────────────────┘

【結果】: 代碼修改 + 測試通過 + 備份完成
```

### 工作流程 C: 新增外掛

```
【使用者】: "幫我新增 payment-gateway 外掛"

【AI 步驟】:
┌─────────────────────────────────────────┐
│ Step 1: 驗證框架                        │
│  確認 .plugin-testing/ 存在             │
│  確認包含所有模板                       │
│                                         │
│ Step 2: 建立項目目錄                    │
│  mkdir payment-gateway                  │
│  mkdir payment-gateway/includes         │
│  mkdir payment-gateway/tests            │
│                                         │
│ Step 3: 複製模板文件                    │
│  bootstrap-unit.php (從模板複製)        │
│  bootstrap.php (從模板複製)             │
│  composer.json (簡化版，指向上層)      │
│  phpunit-unit.xml (簡化版，指向上層)   │
│  TESTING.md (項目說明)                  │
│                                         │
│ Step 4: 初始化 Composer                 │
│  cd payment-gateway                     │
│  composer install                       │
│                                         │
│ Step 5: 建立第一個測試                  │
│  建立 tests/Unit/Services/              │
│         PaymentServiceBasicTest.php     │
│  包含 7 個基礎測試                      │
│                                         │
│ Step 6: 執行測試驗證                    │
│  composer test                          │
│  確保框架正常運作                       │
│                                         │
│ Step 7: 建立備份                        │
│  git add payment-gateway                │
│  git commit -m "新增 payment-gateway 外掛"│
│                                         │
│ Step 8: 完成                            │
│  "payment-gateway 已就緒，可開始開發"   │
└─────────────────────────────────────────┘

【結果】: 新外掛完全初始化，可以開發
```

---

## 3. AI 的工作邏輯

### AI 會自動做的事

```
【環境檢查】(進入項目時)
✓ 確認根目錄是 /Users/fishtv/Development
✓ 確認 .plugin-testing/ 存在
✓ 確認外掛目錄存在
✓ 確認 Composer 已安裝
✓ 確認 PHPUnit 已安裝
✓ 確認 MySQL 可連接 (如需要)

【代碼分析】(修改代碼時)
✓ 讀取修改的文件
✓ 理解改變了什麼
✓ 識別受影響的測試
✓ 預測可能的問題

【測試執行】(驗證時)
✓ 執行 composer test
✓ 捕捉輸出結果
✓ 分析成功/失敗
✓ 提出建議

【備份管理】(保存時)
✓ 執行 git add
✓ 執行 git commit (有意義的信息)
✓ 保存備份點
✓ 允許隨時回滾

【報告】(結束時)
✓ 清楚地說明發生了什麼
✓ 顯示測試結果
✓ 提出下一步建議
✓ 詢問是否需要進一步操作
```

### AI 會詢問的事

```
【修改前】
"我看到代碼有改變。
 修改了什麼？
 要建立備份點再開始測試嗎？"

【測試後】
"測試通過/失敗。
 要建立 git commit 備份嗎？"

【失敗時】
"檢測到 X 個測試失敗。
 我建議:
 1. 修改 XXX
 2. 檢查 YYY
 要我幫你修復嗎？"

【完成後】
"✅ 完成！
 現在有什麼需要？
 - 修改其他代碼
 - 新增測試
 - 提交到 git"
```

---

## 4. 詳細步驟說明

### 場景 1: 「測試代碼」

**你說的話:**
```
「在 Development 目錄下，
 幫我測試 buygo-plus-one」
```

**AI 做的事:**

```
[1] 確認位置
    $ pwd
    /Users/fishtv/Development ✓

[2] 進入項目
    $ cd buygo-plus-one

[3] 檢查狀態
    $ git status
    On branch feature/...
    nothing to commit

[4] 執行測試
    $ composer test

    PHPUnit 9.6.31 by Sebastian Bergmann
    .......
    Time: 00:00.003, Memory: 6.00 MB
    OK (7 tests, 9 assertions)

[5] 報告
    ✅ 所有測試通過 (7/7)

    建議:
    - 繼續開發
    - 新增更多測試
    - 測試其他外掛
```

**結果:** 你知道代碼是安全的

---

### 場景 2: 「修改並驗證」

**你說的話:**
```
「我修改了 ProductService 的 calculatePrice 方法，
 幫我測試並備份」
```

**AI 做的事:**

```
[1] 檢查修改
    $ cd buygo-plus-one
    $ git status

    modified: includes/services/class-product-service.php
    modified: tests/Unit/Services/ProductServiceBasicTest.php

[2] 查看具體改變
    $ git diff includes/services/class-product-service.php

    - public function calculatePrice($items) {
    + public function calculatePrice($items, $discount = 0) {
    ...新增折扣計算...

[3] 執行測試
    $ composer test

    PHPUnit 9.6.31
    .......
    OK (7 tests, 9 assertions) ✅

[4] 建立備份
    $ git add includes/services/class-product-service.php
    $ git add tests/Unit/Services/ProductServiceBasicTest.php
    $ git commit -m "Add discount parameter to price calculation

    - 新增 discount 參數
    - 更新 calculatePrice 方法
    - 新增相應測試"

    [feature/... abc1234] Add discount parameter...
    2 files changed, 8 insertions(+), 3 deletions(-)

[5] 驗證備份
    $ git log --oneline -2

    abc1234 Add discount parameter to price calculation
    def5678 Previous commit

[6] 報告
    ✅ 修改已驗證並備份

    Commit ID: abc1234
    修改文件: 2
    新增行數: 8
    刪除行數: 3

    下一步？
    - 繼續修改
    - 提交更多代碼
    - 新增測試
```

**結果:** 代碼安全 + 完整備份

---

### 場景 3: 「新增外掛」

**你說的話:**
```
「幫我新增一個名叫 'shipping-calculator' 的外掛，
 套用測試框架」
```

**AI 做的事:**

```
[1] 驗證框架
    $ ls -la .plugin-testing/
    ✓ scripts/ 存在
    ✓ templates/ 存在
    ✓ docs/ 存在

[2] 建立項目結構
    $ mkdir shipping-calculator
    $ mkdir shipping-calculator/includes
    $ mkdir shipping-calculator/tests/Unit/Services

[3] 複製模板文件
    $ cp .plugin-testing/templates/bootstrap-unit.php
         shipping-calculator/tests/
    $ cp .plugin-testing/templates/composer.json
         shipping-calculator/
    (自動修改文件名)
    $ cp .plugin-testing/templates/phpunit-unit.xml
         shipping-calculator/

[4] 建立項目特定文件
    $ 生成 shipping-calculator/TESTING.md
    $ 生成 shipping-calculator/.phpunit.config

[5] 初始化 Composer
    $ cd shipping-calculator
    $ composer install

    Loading composer repositories...
    Installing dependencies...
    29 packages installed

[6] 建立第一個測試
    $ 生成 tests/Unit/Services/ShippingServiceBasicTest.php
    (包含 7 個基礎測試)

[7] 執行測試驗證
    $ composer test

    PHPUnit 9.6.31
    .......
    OK (7 tests, 9 assertions) ✅

[8] 建立備份
    $ cd /Users/fishtv/Development
    $ git add shipping-calculator/
    $ git commit -m "Initialize shipping-calculator plugin

    - 建立項目結構
    - 複製框架檔案
    - 新增基礎測試"

    [feature/... xyz9876] Initialize shipping-calculator plugin
    Created mode 100644 shipping-calculator/...

[9] 報告
    ✅ shipping-calculator 已就緒！

    項目位置: /Users/fishtv/Development/shipping-calculator/
    測試狀態: ✅ 全部通過 (7/7)
    初始化 Commit: xyz9876

    可以開始開發了！
```

**結果:** 新外掛完全初始化，可以開發

---

## 5. 備份和回滾

### 自動備份流程

```
修改代碼
    ↓
AI 檢測到改變
    ↓
AI 執行測試
    ↓
如果通過 → AI 詢問「要備份嗎？」
    ↓
使用者確認
    ↓
AI 執行:
  1. git add (相關文件)
  2. git commit (自動生成信息)
  3. git log --oneline -1 (顯示備份點)
    ↓
完成！隨時可以回滾
```

### 查看備份點

```bash
# 看所有備份點
git log --oneline

# 看詳細信息
git log --oneline -10

# 看特定備份的詳情
git show abc1234

# 看改變了什麼文件
git diff abc1234~1 abc1234
```

### 回滾到任何點

```bash
# 回滾到特定備份點
git reset --hard abc1234

# 回滾到上一個備份
git reset --hard HEAD~1

# 驗證回滾成功
git log --oneline -3
composer test  # 確認一切正常
```

---

## 6. 快速參考

### 常見命令

```bash
# 進入根目錄
cd /Users/fishtv/Development

# 查看所有外掛
ls -la

# 進入特定外掛
cd buygo-plus-one

# 執行測試
composer test

# 查看修改
git status
git diff

# 查看備份點
git log --oneline -10

# 回滾
git reset --hard HEAD~1
```

### AI 理解的指令

```
「測試 buygo-plus-one」
  → 執行 composer test

「我改了代碼」
  → 檢查改變 → 執行測試 → 詢問備份

「新增外掛 plugin-name」
  → 完整初始化

「所有外掛都測試」
  → 依次測試每個外掛

「回滾到上一個點」
  → git reset --hard HEAD~1

「查看歷史」
  → git log --oneline -10
```

---

## 📚 下一步

- 進入 `/Users/fishtv/Development`
- 告訴 AI 你想做什麼
- AI 會自動處理一切

**記住:** 只要告訴 AI 你的需求，不用記住任何命令。AI 會完成所有的技術細節。
