# Phase 3.4 執行計劃 - 圖解說明

**任務**: 移除硬編碼 NT$，改用動態幣別

---

## 📊 流程圖解

```
┌─────────────────────────────────────────────────────────────┐
│                    Phase 3.4 執行流程                         │
└─────────────────────────────────────────────────────────────┘

步驟 1: 🔍 搜尋硬編碼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI 執行: grep -r "NT\$" includes/views/pages/
         grep -r "'NT\$ '" includes/
         grep -r '"NT\$ "' includes/

找到檔案:
├── includes/views/pages/shipment-details.php (行 408-416) ← 主要目標
├── includes/services/class-product-service.php (行 529)    ← 也要修改
└── (其他可能的檔案)

         ↓

步驟 2: 📖 讀取檔案
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI 讀取:
├── includes/views/pages/shipment-details.php
└── includes/services/class-product-service.php

查看硬編碼的具體位置和上下文

         ↓

步驟 3: ✏️ 修改程式碼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【修改 1】shipment-details.php (JavaScript 部分)

原始碼 (行 408-416):
┌────────────────────────────────────────────┐
│ const formattedPrice = `NT$ ${price}`;    │ ← 硬編碼
└────────────────────────────────────────────┘

修改為:
┌────────────────────────────────────────────────────────────┐
│ const currencySymbol = window.buygoSettings?.currencySymbol || 'NT$'; │
│ const formattedPrice = `${currencySymbol} ${price}`;                  │
└────────────────────────────────────────────────────────────────────────┘

【修改 2】class-product-service.php (PHP 部分)

原始碼 (行 529):
┌────────────────────────────────────────────┐
│ return 'NT$ ' . number_format(...);        │ ← 硬編碼
└────────────────────────────────────────────┘

修改為:
┌────────────────────────────────────────────────────────────┐
│ $currency = \FluentCart\Api\CurrencySettings::get('currency') ?: 'TWD'; │
│ $symbol = \FluentCart\Api\CurrencySettings::get('currency_symbol') ?: 'NT$'; │
│ return $symbol . ' ' . number_format(...);                                    │
└────────────────────────────────────────────────────────────────────────────────┘

         ↓

步驟 4: 💾 儲存變更
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI 使用 Edit 工具修改檔案
WordPress 透過符號連結立即看到變更 ✅

         ↓

步驟 5: ✅ Git 提交
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AI 執行:
├── git add includes/views/pages/shipment-details.php
├── git add includes/services/class-product-service.php
└── git commit -m "fix: Phase 3.4 - 移除硬編碼 NT$，改用動態幣別"

         ↓

步驟 6: 🧪 你需要測試
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【這部分 AI 做不到，需要你手動測試】

1. 打開 Local by Flywheel
2. 啟動 buygo 站點
3. 瀏覽器: http://buygo.local/wp-admin
4. 檢查以下頁面:
   ├── 出貨管理 (shipment-details.php)
   ├── 商品頁面
   └── Console (F12) 確認沒有錯誤

         ↓

步驟 7: 📝 回報結果
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
你告訴 AI:
├── "測試通過，幣別顯示正確" → 任務完成 ✅
├── "還是顯示 NT$" → AI 檢查問題並修正
└── "Console 有錯誤" → AI 修復錯誤
```

---

## 🎨 修改前後對比

### 修改 1: shipment-details.php

#### ❌ 修改前（硬編碼）
```javascript
// 行 408-416 附近
function formatPrice(price) {
    // 硬編碼台幣符號
    return `NT$ ${price.toFixed(2)}`;
}

// 使用範例
const price = 1000;
const formatted = formatPrice(price);
// 結果: "NT$ 1000.00" ← 永遠是 NT$
```

#### ✅ 修改後（動態）
```javascript
// 行 408-416 附近
function formatPrice(price) {
    // 從全域設定讀取幣別符號
    const currencySymbol = window.buygoSettings?.currencySymbol || 'NT$';
    return `${currencySymbol} ${price.toFixed(2)}`;
}

// 使用範例
const price = 1000;
const formatted = formatPrice(price);
// 結果可能是:
// - "NT$ 1000.00" (台幣)
// - "$ 1000.00" (美金)
// - "¥ 1000.00" (日圓)
// 取決於系統設定 ✅
```

---

### 修改 2: class-product-service.php

#### ❌ 修改前（硬編碼）
```php
// 行 529
private function formatPrice(int $priceInCents): string
{
    // 硬編碼台幣符號
    return 'NT$ ' . number_format($priceInCents / 100, 2);
}

// 使用範例
$formatted = $this->formatPrice(100000); // 100000 分 = 1000 元
// 結果: "NT$ 1,000.00" ← 永遠是 NT$
```

#### ✅ 修改後（動態）
```php
// 行 529
private function formatPrice(int $priceInCents): string
{
    // 從 FluentCart 系統讀取幣別設定
    $currency = \FluentCart\Api\CurrencySettings::get('currency') ?: 'TWD';
    $symbol = \FluentCart\Api\CurrencySettings::get('currency_symbol') ?: 'NT$';

    return $symbol . ' ' . number_format($priceInCents / 100, 2);
}

// 使用範例
$formatted = $this->formatPrice(100000);
// 結果可能是:
// - "NT$ 1,000.00" (台幣)
// - "$ 1,000.00" (美金)
// - "¥ 1,000.00" (日圓)
// 取決於 FluentCart 系統設定 ✅
```

---

## 📁 影響的檔案清單

```
/Users/fishtv/Development/buygo-plus-one/
├── includes/
│   ├── views/
│   │   └── pages/
│   │       └── shipment-details.php  ← 修改 JavaScript
│   └── services/
│       └── class-product-service.php ← 修改 PHP
└── (Git 自動記錄變更)
```

**透過符號連結自動同步到**:
```
/Users/fishtv/Local Sites/buygo/app/public/wp-content/plugins/
└── buygo-plus-one/ (符號連結 → Development)
    └── (所有修改立即生效) ✅
```

---

## 🔍 AI 會搜尋的內容

### 搜尋範圍
```bash
# 搜尋所有可能的硬編碼
grep -r "NT\$" includes/
grep -r "'NT\$ '" includes/
grep -r '"NT\$ "' includes/
grep -r "NT\\$" includes/

# 特別關注的目錄
includes/views/pages/        # 前端頁面
includes/services/            # 服務層
includes/api/                 # API 層
```

### 預期會找到的位置
1. ✅ `shipment-details.php` (行 408-416) - **任務文件已標註**
2. ✅ `class-product-service.php` (行 529) - **我已經看到**
3. ❓ 其他可能的檔案（需要搜尋確認）

---

## 🎯 完整的 Git Commit

```bash
git add includes/views/pages/shipment-details.php
git add includes/services/class-product-service.php

git commit -m "fix: Phase 3.4 - 移除硬編碼 NT$，改用動態幣別

問題:
- shipment-details.php 使用硬編碼 'NT$' (行 408-416)
- class-product-service.php 使用硬編碼 'NT$' (行 529)
- 無法支援多幣別顯示

修改:
- shipment-details.php: 改為讀取 window.buygoSettings.currencySymbol
- class-product-service.php: 改為從 FluentCart API 讀取幣別設定
- 新增後備機制：如果讀取失敗，預設使用 'NT$'

影響範圍:
- 出貨管理頁面
- 商品價格顯示
- API 返回的價格格式

測試:
- 需在 WordPress 後台測試出貨管理頁面
- 確認幣別符號顯示正確
- 確認 Console 沒有錯誤

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## ⏱️ 預估時間

| 步驟 | AI 執行 | 你需要做 | 時間 |
|------|---------|----------|------|
| 1. 搜尋檔案 | ✅ | ❌ | 10 秒 |
| 2. 讀取程式碼 | ✅ | ❌ | 10 秒 |
| 3. 修改程式碼 | ✅ | ❌ | 30 秒 |
| 4. Git 提交 | ✅ | ❌ | 5 秒 |
| 5. 在 WordPress 測試 | ❌ | ✅ | 5-10 分鐘 |
| 6. 回報結果 | ❌ | ✅ | 1 分鐘 |

**總計**: AI 執行 < 1 分鐘，你測試 5-10 分鐘

---

## 🔄 如果測試失敗

### 情境 A: 還是顯示 NT$

**你說**: "還是顯示 NT$"

**AI 會**:
1. 檢查 `window.buygoSettings` 是否有設定
2. 檢查 JavaScript Console 錯誤
3. 調整程式碼
4. 再次請你測試

---

### 情境 B: Console 有錯誤

**你說**: "Console 顯示錯誤: xxx"

**AI 會**:
1. 分析錯誤訊息
2. 修正問題（語法錯誤、變數名稱等）
3. 再次請你測試

---

### 情境 C: 某些頁面正常，某些不正常

**你說**: "出貨頁面正常，但商品頁面還是 NT$"

**AI 會**:
1. 搜尋商品頁面的相關檔案
2. 找到並修改商品頁面的程式碼
3. 再次請你測試

---

## ✅ 測試檢查清單

請你在 WordPress 後台檢查：

### 出貨管理頁面
- [ ] 打開出貨管理頁面
- [ ] 確認價格顯示格式正確
- [ ] 確認幣別符號正確（如果系統設定是台幣，應顯示 NT$）
- [ ] 打開 Console (F12)，確認沒有紅色錯誤

### 商品頁面
- [ ] 打開商品列表
- [ ] 確認商品價格顯示正確
- [ ] 確認幣別符號正確

### Console 檢查
- [ ] 沒有 `undefined` 錯誤
- [ ] 沒有 `currencySymbol is not defined` 錯誤
- [ ] 沒有其他 JavaScript 錯誤

---

## 🎉 完成條件

當以下條件**全部**滿足時，任務才算完成：

1. ✅ 程式碼已修改（AI 完成）
2. ✅ Git 已提交（AI 完成）
3. ✅ WordPress 測試通過（你完成）
4. ✅ Console 沒有錯誤（你確認）
5. ✅ 所有頁面幣別顯示正確（你確認）

---

## 🚀 準備開始了嗎？

如果你說：**"好，開始執行"**

我會立即：
1. 🔍 搜尋所有硬編碼 NT$
2. 📖 讀取相關檔案
3. ✏️ 修改程式碼
4. 💾 Git 提交
5. 📋 列出測試清單給你

然後等你測試完回報結果！

---

**問題？**
- 想看其他任務的執行計劃？
- 有哪個步驟不清楚？
- 想要更詳細的說明？

隨時告訴我！ 😊
